# CLAUDE.md

Контекст проекта для Claude Code. Прочитай этот файл перед началом работы.

## Что это за проект

Solo Leveling Daily Training (SLDT) — локальный фитнес-трекер ежедневных заданий в визуальной стилистике манхвы/аниме Solo Leveling. Это **трекер с игровыми элементами**, а не игра с элементами трекера — важное разграничение, зафиксированное с пользователем явно: никаких виртуальных событий (подземелья, врата, битвы с монстрами и т.п.), только реальные упражнения (отжимания, приседания, подтягивания, пресс, шаги) с игровой надстройкой (уровни, ранги, титулы, артефакты, магазин).

Репозиторий на GitHub: `solo-leveling-tracker`.
Текущая версия: **v5.9.36**.

## Технологии и структура файлов

Никакого билд-процесса, фреймворков или npm-зависимостей. Чистый HTML/CSS/JS.

```
index.html      — всё приложение: разметка, стили (<style>), логика (<script>) в одном файле
manifest.json   — PWA-манифест
sw.js           — Service Worker (кэширует приложение + иконки + шрифт Orbitron для офлайна)
icons/          — иконки приложения (сгенерированы из исходного логотипа, разные размеры/назначения)
```

Все данные пользователя хранятся в `localStorage` (два ключа: состояние прогресса и суммарная статистика за всё время). Никакого бэкенда, никакой синхронизации между устройствами — экспорт/импорт прогресса через файл `.json` или код в буфере обмена (обе опции живут в модалке «АРХИВ»).

## Версионирование и совместимость сохранений

- Номер версии указан в `<title>` и в `#limitBreakContainer`/шапке — держи его синхронизированным при правках.
- Ключи `localStorage` версионируются (`sl_daily_vX_Y_Z`, `sl_total_stats_vX_Y_Z`). Список `PREV_KEYS`/`PREV_STATS_KEYS` в начале скрипта — цепочка миграции старых сохранений на новый ключ.
- **Бампать ключ хранения нужно только при изменении структуры данных**, а не при чисто косметических/визуальных правках — иначе накапливается мусор миграций. Чисто CSS/текстовые правки (примеры — v5.5.1–v5.5.7) номер ключа не меняли.
- При добавлении нового поля в объект `data`: обязательно добавить его (а) в дефолт для чистой установки, (б) в блок миграции старых сохранений с фолбэком, (в) в защитную нормализацию сразу после загрузки (на случай кривых/старых данных), (г) в `sanitizeImportedData()` для валидации при импорте бэкапа.
- `sw.js` имеет свою переменную `CACHE_VERSION` — её нужно бампать при каждом изменении любого закэшируемого файла (`index.html`, иконки, манифест), иначе часть пользователей будет видеть версию из кэша.

## Ключевые игровые системы (кратко, для ориентира)

- **Квесты**: 5 заданий в день (pushups/squats/pullups/press/steps), цель на день считается через `getDynamicTarget()` — растёт с уровнем, но с уровня 1 по 10 действует щадящий множитель (0.5×→1.0× линейно, `getBeginnerMultiplier()`), дальше — обычный рост. Награда (`getDynamicExp()`) от этого множителя **не зависит** — награда всегда полная, это осознанное решение.
- **Уровни/ранги**: EXP → левел-ап → ранг (E/D/C/B/A/S/National/SSS/Shadow Monarch) через `getRank()`.
- **Титулы**: иерархия значимости `TITLE_PRIORITY` (важнее → менее важно). Новый титул авто-надевается только если он значимее текущего активного; иначе просто добавляется в список. Ручной выбор — клик по карточке в Инвентаре. «Целеустремлённый» требует непрерывной серии 7 дней и полностью пропадает при её прерывании.
- **Преодоление предела**: разблокируется с 3 уровня (кнопка физически скрыта до этого момента), 2 использования в день (плюс бонусы от артефакта/статов/расходников — см. версии v5.8.0+), сбрасывается ежедневно. Расположена выше сетки 2×3 (между последним упражнением и кнопками), визуально отделена разделительной линией.
- **Система штрафов**: пропуск дня снимает % EXP (зависит от уровня/силы/артефактов), показывает модальный баннер с суммой. Руна Защиты — расходник, отменяет штраф на следующий день.
- **Характеристики**: пять статов — Сила (STR), Ловкость (AGI), Выносливость (STA), Интеллект (INT), Восприятие (PER), в этом порядке на экране. Очки начисляются при левел-апе (база + бонусы от артефактов роста статов), распределяются вручную. Подробности эффектов каждого стата и цифры — см. записи v5.9.9–v5.9.10.
- **Системные уведомления**: 3 типа (утро/срочно/выполнено), у каждого 2–3 обезличенных варианта текста (Система обращается как «Игрок», никогда не по имени — так по лору первоисточника). Показываются не чаще раза в день, флаги в `data.dailyNotices`.
- **Имя игрока**: спрашивается один раз при первой установке (не при миграции старых сохранений), хранится в `data.playerName`, можно сменить из «АРХИВА». Фолбэк-подпись — «Игрок» (не «Охотник» — сознательно отвергнутый вариант).
- **Журнал/календарь («Летопись»)**: отдельная кнопка (не вкладка — так было в v5.6.0, переделано в v5.7.0). `data.history` — запись на каждый день (статус success/penalty/protected + опционально достигнутый уровень), ключ — календарная дата **начала** цикла заданий (сдвиг через полночь до 5 утра учтён в `checkMissedDays()`, дата берётся от `data.lastReset` до его обновления). `data.bestStreak` — рекорд серии, обновляется в `applyPenaltyLogic()`. `data.pendingLevelUpToday` — служебное поле, перезаписывается на каждый левел-ап в течение дня, «прикрепляется» к записи истории при суточном сбросе и обнуляется. История ведётся с нуля от даты обновления (ретроактивно не восстанавливается — это ожидаемо). Цвета: зелёный/красный/синий — обводка+свечение (не заливка), сегодняшний день — рамка из 4 уголков синим. Пурпурный в календаре не используется — он закреплён только за рангом Shadow Monarch.
- **Кодекс Системы**: отдельная кнопка, список глав (`CODEX_CHAPTERS`) с **индивидуальной нумерацией** — `data.codexUnlocked` хранит порядок открытия лично у этого игрока (не привязан к теме), поэтому дыр в номерах не бывает. Базовые главы (`daily_quota`, `penalty`, `chronicle`, `shop`, `boxes`, `archive`, `stats`) есть у всех с самого начала без уведомления. Остальные главы (ранги через `RANK_CODEX_MAP`, артефакты `artifact_*`, три титула через `TITLE_CODEX_MAP`, `limit_break` — с 3 уровня) открываются функцией `unlockCodexChapter(id)`, которая уведомляет («Записано в Кодекс: …») и является идемпотентной (безопасно вызывать повторно). Чтобы добавить новую главу под будущий предмет/фичу — просто дописать запись в `CODEX_CHAPTERS` и вызвать `unlockCodexChapter()` в нужном месте, механизм это и есть весь его смысл.
- **Прогресс**: отдельная кнопка, показывает то, что раньше было текстом на главном экране («Общий прогресс за всё время»).
- **Нижние кнопки**: сетка 2×3 (`sub-menu-grid`) — Кодекс/Летопись/Архив, Магазин/Инвентарь/Прогресс.
- **Очередь системных уведомлений**: единый механизм (`enqueueNotification`/`processNotificationQueue`/`displayNotificationNow`) для ВСЕХ типов сообщений (короткие баннеры через `showNotice()`, объявления о ранге/титуле через `showSystemPopup()`, Кодекс). Не более одного на экране одновременно, автозакрытие через 10 секунд или тап раньше, пауза 2 секунды между показами. Не путать с `.levelup-popup`/`.complete-popup` — те не участвуют в очереди, это отдельные визуальные эффекты.
- **Журнал уведомлений за сутки**: `data.notificationLog` (время + текст, самые новые сверху), пишется в `logNotification()` при каждом показе, сбрасывается при суточном сбросе. Доступ — кнопка «Уведомления системы» в «АРХИВЕ» (секция «Игрок»).
- **Напоминания о невыполненном задании**: категория переименована из «Срочно» в «reminder» (в коде: `SYSTEM_NOTICES.reminder`/`reminderUrgent`). Проверяется каждый час в окне 10 часов до сброса (`checkReminderNotice()`, `data.remindersFiredHours` — какие часовые отметки уже показаны сегодня), последние 2 часа — более настойчивый пул текстов. Прекращается сразу при выполнении задания.
- **Защита от повтора текста**: `pickRandomNoRepeat(pool, category)` — не показывает тот же текст, что был в прошлый раз в той же категории (`data.lastNoticeText`). Используется для morning/complete/reminder.

**Стилистика Кодекса (важное правило для будущих глав):** текст в `CODEX_CHAPTERS` — финальный, согласован с пользователем построчно (v5.7.1). Любые новые главы должны выдерживаться в той же стилистике:

**Уроки (не повторять):**
- Не переделывать модальные окна (`.status-window`) на схему «закреплённый заголовок / скролл только тела / закреплённая кнопка» через `overflow:hidden` на окне + `dvh`/JS-подстраховку высоты — пробовали в v5.7.3–v5.7.5, ломало мобильный Chrome (контент вылезал за окно, кнопка «Закрыть» переставала работать). Текущая рабочая архитектура (с v5.7.8) — закреплённый заголовок, скроллится только `.status-body`, кнопка «Закрыть» и HUD-уголки вне зоны прокрутки, **без** `overflow:hidden`/`dvh`/JS-подстраховки.
- При диагностике по скриншоту — всегда уточнять, какой именно файл на скриншоте (реальное приложение или отдельный демо-макет для показа эффекта) — путаница с этим один раз стоила целой сессии ложной отладки несуществующего бага.

**v5.7.8:** от архитектуры v5.7.3 убрана только полоса прокрутки (`scrollbar-width: none` + `::-webkit-scrollbar { display: none }` на `.status-body`) — больше ничего не менялось. Оба HUD-уголка (`::before` и `::after`) сохранены — в этой архитектуре они не участвуют в прокрутке, поэтому проблема, ради которой предлагалось убирать нижний уголок, тут не возникает.

**`APP_VERSION`** — JS-константа (в начале скрипта, рядом с `STORAGE_KEY`) с номером текущей версии в виде строки (например, `'5.7.9'`). Обновлять её при каждом бампе версии — вместе с `<title>`, `CACHE_VERSION` в `sw.js` и этим файлом. Отображается тихой строкой в модалке «АРХИВ» (раздел «Версия», над «Игрок») — это позволяет пользователю быстро сверить на телефоне, что открылась именно новая версия, а не старая закэшированная.

**Уведомления о прогрессе дня (20/40/60/80%)** — `countDoneQuests()` считает, сколько из 5 заданий закрыто. Каждый раз, когда одно из пяти упражнений достигает своей цели (в обработчике клика `+`), проверяется `countDoneQuests()*20`; если такой процент ещё не показывался сегодня (`data.progressNoticesShown`) и это не 100% (100% уже покрыто существующей категорией «Выполнено»/`dailyNotices.complete`) — показывается `showNotice('Задание дня выполнено на X%.', 'info')`. Один шаблон текста на все пороги, без вариаций. Массив `progressNoticesShown` сбрасывается ежедневно вместе с остальными daily-полями; не сбрасывается при активации «Преодоления лимита» (специально, чтобы повторное прохождение дня не спамило те же уведомления заново).

**v5.7.10:**
- `user-select: none` на всех `<button>` — чинит случайное выделение текста/меню «Копировать» при быстрых тапах на мобильном.
- При первом запуске (чистая установка) окно ввода имени открывается **до** запуска `checkMissedDays()`/таймера/уведомлений (`finishInit()` вызывается только после `skipPlayerName()`/`submitPlayerName()`) — раньше они могли открыться одновременно и наложиться друг на друга.
- **Служебная панель «Длань архитектора»** — вызов: 7 тапов по номеру версии в Архиве за 2 секунды (`onVersionPulse()`, делегированный обработчик на `#backupContent`). Даёт инструменты для тестирования: добавить EXP/кредиты, установить уровень напрямую, быстрые +1 уровень/+1000 EXP, принудительно применить штраф (с показом баннера), обнулить счётчик штрафов, сбросить дневной прогресс, симулировать полный сброс суток (сдвигает `data.lastReset` на -24ч и вызывает настоящий `checkMissedDays()`, так что весь обычный пайплайн — Летопись/уведомления/штраф — срабатывает по-настоящему), сбросить историю Летописи. Для тестов; весь код помечен маркером `SL:SYS-TOOLS` — вырезать эти блоки перед полностью публичной витриной.

**v5.7.11 — ревизия баланса прокачки (важно для будущих правок).**

Проблема, которая привела к этой правке: старая формула опыта (`1000 × 1.15^уровень`, экспоненциальная) в паре с бесконечно растущими дневными целями (`+5%/уровень`, тоже без потолка) означала, что после ~30 уровня прогрессия математически упиралась в стену — к 50 уровню на один левел-ап уходило больше года, к 70 — годы жизни, к 100 — сотни лет. Подробный разбор с таблицей цифр — см. историю чата, здесь только итоговые формулы.

**Кривая опыта** (`getExpToNext`) — заменена на линейную: `150 × уровень`. Откалибровано симуляцией: ~375 дней (~1 год) до 100 уровня при 100% ежедневной дисциплине, ~1100+ дней (~3 года) при выполнении примерно трети дней. Долгосрочно (за пределами 100 уровня) темп сам стабилизируется на ~5-6 днях за уровень и не взрывается повторно — это математическое следствие того, что награда (`getDynamicExp`) продолжает расти линейно с уровнем, а стоимость уровня теперь тоже линейна.

**Дневные цели** (`getDynamicTarget`, через `getTargetGrowthMultiplier`) — рост **без потолка** (сознательное решение — вдохновлено идеей прогрессивной нагрузки, не должно "упираться в стену"), но в два неспешных темпа:
- 1-10 ур.: щадящая кривая новичка (`getBeginnerMultiplier`, 0.5×→1.0×) — без изменений.
- 10-100 ур.: `+1.4%` за уровень.
- 100+ ур.: `+0.7%` за уровень (вдвое медленнее — "для самых упорных").

Референсные значения (база: отжимания 100 / приседания 120 / подтягивания 40 / пресс 100 / шаги 10000):

| Уровень | Отжимания | Приседания | Подтягивания | Пресс | Шаги |
|---|---|---|---|---|---|
| 10 | 100 | 120 | 40 | 100 | 10 000 |
| 30 | 128 | 153 | 51 | 128 | 12 800 |
| 50 | 156 | 187 | 62 | 156 | 15 600 |
| 100 | 225 | 271 | 90 | 225 | 22 600 |
| 200 | 296 | 355 | 118 | 296 | 29 600 |

**Награда за задание** (`getDynamicExp`) — намеренно **не менялась**, продолжает расти как раньше (`+5%/уровень`, без потолка, плюс бонусы от INT/титулов). Это математически необходимо для того, чтобы кривая опыта не взорвалась повторно на высоких уровнях (см. выше).

**Штраф и STR — "Вариант Б".** Раньше штраф почти обнулялся уже к 50 уровню независимо от вложений в STR (снижение от уровня само по себе пробивало минимальный порог в 1%). Теперь:
- Снижение от уровня: `⌊уровень÷20⌋ × 3%` (было `÷10` — вдвое медленнее).
- Снижение от STR: `0.25%` за очко выше 10 (было `0.5%` — вдвое медленнее).
- Минимальный порог: **5%** (было 1%).
Формула продублирована в двух местах — `applyPenaltyLogic()` (реальный штраф) и в служебной панели (блок `SL:SYS-TOOLS`, сейчас `applyPenaltyAction()`) — держать в синхроне при любых будущих правках баланса.

**INT и AGI** — сознательно не трогались в этой сборке (INT ускоряет прокачку опыта при вложении, AGI даёт шанс крита по кредитам — оба воспринимаются как осмысленные бонусы за вложение, а не системная дыра, в отличие от STR/штрафа).

**Отложено на следующую сборку (явно так решено, не забыть):** ~~новые артефакты/расходники для магазина~~ — **закрыто в v5.9.0** (вся предметная система — шкатулки/артефакты/расходники/ключи — полностью реализована к этой версии). Экономика штрафов через свитки/кредиты "про запас" (пользователь отметил: можно копить кредиты и превентивно гасить штрафы расходником — это тоже рычаг баланса) — осталась как есть, отдельно не пересматривалась.

**v5.7.12 — донастройка старта и стартовое уведомление.**

- **Стартовое уведомление** — категория «Утро» (был пул из 9 случайных текстов) заменена на единственный фиксированный текст `STARTUP_NOTICE_TEXT`: «Получено задание на день. Невыполнение приведёт к штрафу.» Показывается и при первом запуске, и при каждом обычном суточном сбросе. Триггер в `checkMissedDays()` теперь завязан только на флаг `data.dailyNotices.morning` (убрал условие `dayRolled`, которое на чистой установке никогда не становится `true`, из-за чего стартовое сообщение раньше на первом запуске вообще не показывалось — это и была часть бага).
- **Задержка 3 секунды на первом запуске** — `skipPlayerName()`/`submitPlayerName()` теперь вызывают `setTimeout(finishInit, 3000)` вместо мгновенного `finishInit()`, чтобы стартовое уведомление не наслаивалось визуально на закрывающееся окно «Система активирована». В обычные дни (не первый запуск) `finishInit()` вызывается сразу, без задержки — там нет модалки, с которой можно столкнуться.
- **Смягчение уровней 1-2** — `getExpToNext()` использует ручные пороги для первых двух уровней вместо общей формулы `150×уровень`: 1→2 = 1000 опыта, 2→3 = 1500 опыта. С 3 уровня — без изменений. Иначе при полном выполнении всех заданий в первый же день игрок мгновенно перепрыгивал на 3 уровень (открывая Преодоление лимита в день установки) — слишком щедро для самого старта. Теперь: 2 уровень — день 2, 3 уровень (и разблокировка Лимита) — день 5, при ежедневном 100% выполнении.
- **Уточнение по темпу «3 года» до 100 уровня** (из обсуждения, важно помнить при следующих правках баланса): это не «выполнение 1 дня из 3», как считалось изначально при грубой прикидке — с учётom реального штрафа за пропуски (который не просто не даёт опыт, а ещё и списывает уже накопленный) это соответствует темпу **«через день» (50% дней)**. Более редкое выполнение (раз в 3-4 дня) при текущих формулах даёт кратно больше 3 лет вплоть до полного застревания прогресса — это осознанно принято как честный эффект работы штрафа, не баг.

**v5.7.13 — задержка уведомлений относительно анимаций.** `.complete-popup` (1.8с) и `.levelup-popup` (3с) — чисто визуальные эффекты, не участвующие в очереди уведомлений; раньше системные уведомления (проценты/выполнено/ранг/лимит/кодекс/титул) вставали в очередь сразу и визуально накладывались на эти анимации. Теперь показ соответствующих уведомлений отложен на **4.5 секунды** от момента клика/левел-апа (через `setTimeout(..., 4500)`) — единая задержка для всех, с запасом покрывает обе анимации. **Важно:** задержан только вызов `showNotice()`/`showSystemPopup()` (то есть что видит пользователь) — все связанные изменения данных (`data.progressNoticesShown`, `data.dailyNotices.complete`, `data.codexUnlocked`, `data.titlesUnlocked`, `data.notifiedLimitBreakUnlock`) по-прежнему происходят синхронно и сохраняются немедленно через `saveData()`, чтобы не потерять прогресс, если пользователь закроет вкладку в эти 4.5 секунды.

**v5.7.14 — два связанных фикса вокруг клика по «+».**
- **Баг «0%».** В обработчике клика `donePct`/`allQuestsDone()` раньше вызывались **после** `checkLevelUp()`. Раз `countDoneQuests()`/`allQuestsDone()` пересчитывают цели всех 5 заданий заново от `data.level` — если именно этот клик попутно вызывал левел-ап, цели задним числом подскакивали, и уже засчитанные сегодня задания могли "перестать" считаться выполненными по новым, более высоким порогам (в худшем случае показывало «Задание дня выполнено на 0%», хотя реально всё было закрыто). Исправлено: `donePct`/`wasAllDone` теперь снимаются **до** вызова `checkLevelUp()` — фиксируют состояние на момент реального выполнения, левел-ап это больше не портит.
- **Уведомление о каждом левел-апе.** Раньше текстовое системное уведомление о повышении уровня показывалось только если левел-ап попутно менял ранг/открывал лимит — обычный левел-ап отмечался только большой анимацией `LEVEL UP` (которая не пишется в журнал уведомлений). Теперь каждый левел-ап (внутри `checkLevelUp()`, при `leveled === true`) дополнительно ставит в очередь `«Достигнут N уровень.»` (N — итоговый уровень после всего цикла while, если апнулось сразу несколько уровней за раз — показывается один раз, финальный уровень) — с той же задержкой 4.5с, и остаётся в журнале «Уведомления системы» в Архиве.

**v5.7.15 — Этап 1 предметов: шкатулки, стопки, новые расходники (иконки — пока emoji-заглушки, SVG/арт отдельным заходом позже).**

**Модель данных изменена без миграции старого формата** (пользователь явно разрешил — до этой версии инвентарь фактически пустовал, сохранять было нечего):
- `data.consumables` был массивом повторяющихся id (`['elixir','elixir','scroll']`) → стал объектом-счётчиком (`{elixir: 2, scroll: 1}`). Стопки — часть модели данных, а не трюк при отрисовке.
- `data.boxes` (новое поле) — тот же принцип, объект-счётчик по id шкатулки.
- Три новых булевых поля: `data.targetDiscountToday`, `data.expBoostToday` (оба сбрасываются в `checkMissedDays()` ежедневно), `data.streakShield` (НЕ сбрасывается по дням — расходуется только при реальном срыве, см. ниже).
- `sanitizeImportedData()` обновлена под новый формат (проверяет `consumables`/`boxes` как объекты с числовыми значениями по известным id из `SHOP_CATALOG`/`BOX_CATALOG`, не массивы).

**Переименования** (внутренний `id` в `ARTEFACTS`/`SHOP_CATALOG` не менялся — только `name`, чтобы не трогать остальную логику):
- `ring_speed`: Кольцо Спешки → **Кристалл Ускорения**
- `medal_dev`: Медаль Развития → **Печать Развития**
- `elixir`: Эликсир Очищения → **Руна Очищения**
- `scroll`: Свиток Иммунитета → **Руна Защиты**
- `potion`: Зелье Стойкости → **Зелье Восстановления**
- Соответствующие главы Кодекса (`artifact_ring_speed`, `artifact_medal_dev`) — текст обновлён под новые названия.

**Три новых расходника** (доступны в Магазине сразу + могут выпадать из шкатулок):
- `freedom_rune` (Руна Освобождения, 700 кредитов) — `data.targetDiscountToday = true` → `getDynamicTarget()` домножает результат на 0.75 (−25%) до конца дня.
- `stability_rune` (Руна Стабильности, 1200 кредитов) — `data.streakShield = true` → в `applyPenaltyLogic()` при срыве дня, если щит активен, серия НЕ сбрасывается, а щит гасится (`streakShield = false`); если щита нет — обычный сброс `consecutiveDays = 0`.
- `growth_potion` (Зелье Роста, 600 кредитов) — `data.expBoostToday = true` → `getDynamicExp()` добавляет +0.20 к множителю опыта до конца дня.

**Шкатулки** (`BOX_CATALOG`, пока только 2 тира — Этап 1):
- `box_basalt` (Базальтовая, вес 70) и `box_onyx` (Ониксовая, вес 30) — выбор тира через `rollBoxTier()`, взвешенный случайный выбор среди списка из `getAvailableBoxTiers()` (сознательно вынесено в отдельную функцию — на будущих этапах новые тиры добавляются туда, с гейтингом по уровню, без переделки остальной логики).
- **Выпадение:** ровно одна шкатулка за **каждый** пройденный уровень — цикл внутри `while` в `checkLevelUp()`, если апнулось несколько уровней за один раз, шкатулок будет несколько (может быть разных тиров). Уведомление о полученных шкатулках сгруппировано по тиру (`«Получен предмет: Базальтовая Шкатулка ×2»`) и идёт с той же задержкой 4.5с, что и остальные left-up-уведомления.
- **Открытие** (`openBox`/`performOpenBox`) — высыпает 1–3 случайных расходника из `BOX_LOOT_POOLS[boxId]` (простое взвешивание через повтор id в массиве — не «честная» вероятностная таблица, но для Этапа 1 достаточно; можно уточнить точные веса на будущих этапах). Каждый выпавший предмет — отдельное уведомление `«Получен предмет: [Название]»`.

**Инвентарь — новый вид:** добавлен раздел «Шкатулки» (между Артефактами и Расходниками), Расходники теперь тоже отображаются стопками (`«Руна Очищения ×3»`) вместо отдельной строки на каждый экземпляр.

**Эффект использования/открытия** (`flashThenRun()`) — общая функция: находит DOM-карточку предмета по `data-consumable-id`/`data-box-id`, добавляет класс `.item-flash` (голубое свечение, CSS-анимация `itemFlash`, 0.6с), и только **после** этой паузы выполняет реальное изменение данных (списание из стопки, эффект, `saveData()`, `render()`, `openInv()`). Если карточка не найдена в DOM — эффект просто пропускается, логика всё равно выполняется (защита от ошибок).

**Не вошло в Этап 1 (следующими заходами):** расходные руны (усиленные разовые версии), уникальные расходники, оставшиеся артефакты (Печать Предела, Кристалл Прозрения, Амулет Непрерывности, Сфера Роста, Кристалл Теней), средние/высокие шкатулки (Обсидиановая, Алая, Багровая, Тёмного Кварца, Пурпурная, Шкатулка Теней), SVG/арт-иконки вместо emoji.

**v5.7.16 — переименование внутренних id предметов + эффект покупки.**

Пользователь заметил рассинхрон: названия предметов (`.name`) переименовали в v5.7.15, а внутренние технические id (`ring_speed`, `medal_dev`, `elixir`, `scroll`, `potion`, `freedom_rune`, `stability_rune`, `growth_potion`) остались старыми — решили привести к единообразию. Таблица переименования id (все ссылки везде в коде обновлены — `ARTEFACTS`/`SHOP_CATALOG` ключи, `CODEX_CHAPTERS` ключи `artifact_*`, `BOX_LOOT_POOLS`, `data.inventory.includes(...)`-проверки, `performUseItem`'s `if (id === ...)` цепочка):

| Старый id | Название | Новый id |
|---|---|---|
| `ring_speed` | Кристалл Ускорения | `crystal_speed` |
| `medal_dev` | Печать Развития | `seal_growth` |
| `elixir` | Руна Очищения | `rune_cleansing` |
| `scroll` | Руна Защиты | `rune_protection` |
| `potion` | Зелье Восстановления | `potion_restoration` |
| `freedom_rune` | Руна Освобождения | `rune_freedom` |
| `stability_rune` | Руна Стабильности | `rune_stability` |
| `growth_potion` | Зелье Роста | `potion_growth` |
| `amulet_will` | Амулет Воли | без изменений (уже совпадал) |

Схема нейминга на будущее — `тип_суть` (`rune_...`, `potion_...`, `crystal_...`, `seal_...`) — придерживаться и для новых предметов следующих этапов.

**Эффект при покупке в Магазине** — по аналогии с использованием/открытием (`flashThenRun()`), но **золотой** вспышкой (`.item-flash-gold`/`itemFlashGold`, отдельный keyframe от голубого `.item-flash`) — золотой, потому что это получение нового, как и другие "золотые" системные моменты (левел-ап, ранг, титул), в отличие от голубого — "применение эффекта". `flashThenRun()` теперь принимает необязательный третий параметр — имя CSS-класса вспышки (по умолчанию `'item-flash'`).

**Иконки предметов — статус на конец сессии:** договорились об итоговом технических требованиях к файлам (готовит пользователь сам, генерирует нейросетью + обрабатывает в Photoshop):
- Размер: **256×256px**, квадрат, с отступом ~10-15% от краёв (объект занимает ~70-80% кадра).
- Формат: **PNG-24 с альфа-каналом** (прозрачный фон, не PNG-8).
- Имена файлов — по новым английским id из таблицы выше, `.png`: `crystal_speed.png`, `amulet_will.png`, `seal_growth.png`, `rune_cleansing.png`, `rune_protection.png`, `potion_restoration.png`, `rune_freedom.png`, `rune_stability.png`, `potion_growth.png`, `box_basalt.png`, `box_onyx.png`, плюс титулы: `title_novice.png`, `title_awakened.png`, `title_determined.png`, `title_limitbreaker.png`, `title_wanderer.png`.
- Мои собственные попытки нарисовать иконки (сначала SVG вслепую, потом PIL-растр с визуальной проверкой через `view`) пользователь признал слишком примитивными для этой задачи — решили, что пользователь генерирует сам во внешней нейросети (пробовали Grok) и присылает готовые PNG.
- Стилевой промпт прошёл две итерации: сначала "детальный полу-реалистичный рендер" — оказался слишком детализирован для мелкой иконки (мелкие трещины/искры/лучи превращаются в кашу на 40px). Второй, актуальный вариант промпта — "bold simple iconic silhouette", явно просит убрать мелкую деталь, читаемость на маленьком размере — это финальное направление, но ещё не подтверждено на практике (пользователь ещё не сгенерировал этот вариант).
- **На будущее (для более высоких тиров шкатулок — Алая/Багровая/Пурпурная):** решено, что открытие требует соответствующего цветного **ключа** в инвентаре, который расходуется при открытии — отдельный новый тип предмета, ещё не реализован (относится к следующим этапам, не Этапу 1).
- Когда пользователь пришлёт готовые PNG — просто положить файлы в `icons/items/` (по аналогии с `icons/` для иконок самого приложения) и заменить emoji на `<img>` в разметке карточек, не забыть добавить новые файлы в `APP_SHELL` в `sw.js` для офлайн-кэширования.

**v5.7.17 — два фикса вокруг уведомлений о получении предметов.**
- **Шкатулки: одно уведомление вместо нескольких.** `performOpenBox()` раньше показывал отдельное `showNotice()` на каждый выпавший предмет по очереди (через общую очередь, с задержками между показами). Теперь все предметы за одно открытие группируются (с учётом повторов — `«Название ×2»`) и показываются **одним** уведомлением: `«Получен предмет: Руна Очищения, Зелье Роста ×2»`.
- **Артефакты: добавлено уведомление о получении + исправлен порядок.** Раньше при получении артефакта через левел-ап показывалось только «Записано в Кодекс: …» — самого «Получен предмет: [артефакт]» не было. Хуже того, порядок регистрации был неверным: `unlockCodexChapter()` для артефакта вызывался **внутри** `while`-цикла (до того, как в `if(leveled)` регистрируется уведомление об уровне) — так как оба уведомления используют одинаковую задержку `setTimeout(...,4500)`, а браузер запускает такие таймеры в порядке их регистрации, Кодекс-уведомление реально всплывало **раньше** уведомления об уровне. Исправлено: `checkLevelUp()` теперь копит `gainedArtefacts` (аналогично уже существующему `gainedBoxes`) во время `while`-цикла, не вызывая никаких уведомлений сразу; сами уведомления регистрируются строго по порядку **после** цикла, внутри `if(leveled)`: сначала «Достигнут N уровень», затем ранг/лимит (если применимо), затем для каждого артефакта — «Получен предмет: [название]» и сразу вслед за ним `unlockCodexChapter()` (который сам регистрирует «Записано в Кодекс: …»). Итоговый порядок показа: **уровень → предмет → Кодекс**, как и было явно согласовано с пользователем. Этот же паттерн (копить в массив во время цикла, регистрировать уведомления строго по порядку после цикла) — общее правило для любых будущих добавлений в `checkLevelUp()`, чтобы не наступать на те же грабли с порядком одновременных `setTimeout`.

**v5.7.18 — реальные иконки вместо emoji (16 штук, Этап 1).**

Пользователь сгенерировал иконки во внешней нейросети (все 48 штук по общему списку — не только Этап 1) — присланы как 256×256 PNG с однородным чёрным фоном (не настоящей прозрачностью). Обработка: `unmultiply_from_black` (для каждого пикселя `alpha = max(r,g,b)`, цветовые каналы переразделены на этот alpha) — даёт чистое удаление фона с мягкими краями у светящихся элементов, без грубой обрезки по порогу. Все 48 обработанных файлов лежат в `icons/items_pending/` (про запас, не используются кодом, ждут будущих этапов), 16 нужных для уже реализованных предметов скопированы в `icons/items/` (используются кодом, добавлены в `APP_SHELL` в `sw.js` для офлайн-кэша).

**Размеры в интерфейсе:**
- Титулы — 24×24px (22px на мобильном брейкпоинте), НЕ кликабельны — это статус-бейдж, а не предмет для разглядывания.
- Артефакты/расходники/шкатулки — 32×32px (28px на мобильном) в списках, **кликабельны**.

**Клик по иконке предмета** (`showItemPreview(iconId, name)`) — открывает `#itemPreviewOverlay`: сама иконка 200×200px + подпись с названием, в общем HUD-стиле окна. Закрывается тапом в любом месте оверлея (включая саму картинку — не нужна отдельная кнопка «Закрыть», обработчик клика висит на всём оверлее целиком). Для титулов такого клика нет.

**Сопоставление файл ↔ каталог:** для артефактов/расходников/шкатулок имя файла **совпадает** с внутренним id в `ARTEFACTS`/`SHOP_CATALOG`/`BOX_CATALOG` (`icons/items/${id}.png`) — никакой дополнительной map-таблицы не нужно. Для титулов имя файла **не совпадает** с русским названием — есть отдельная таблица `TITLE_ICON_IDS` (русское название → id файла), т.к. использовать русские названия в путях к файлам не хотелось.

**Полный список всех 48 запланированных файлов** (не только Этап 1) — по категориям: Титулы (5), Кристаллы (6: `crystal_speed`✅, `crystal_insight`, `crystal_shadow`, `crystal_restoration`, `crystal_clarity`, `crystal_impulse`), Печати (2: `seal_growth`✅, `seal_limit`), Зелья (3: `potion_restoration`✅, `potion_growth`✅, `potion_restoration_full`), Сферы (1: `sphere_growth`), Руны (15: `rune_cleansing`✅, `rune_protection`✅, `rune_freedom`✅, `rune_stability`✅, плюс 4 расходных заряженных версии и 7 уникальных), Амулеты (2: `amulet_will`✅, `amulet_continuity`), Осколки (2: `shard_limit`, `shard_limit_double`), Шкатулки (8: `box_basalt`✅, `box_onyx`✅, плюс 6 более высоких тиров), Ключи (4: `key_scarlet`, `key_crimson`, `key_purple`, `key_shadow` — для будущей механики "цветная шкатулка требует одноимённый ключ, ключ расходуется при открытии"; ещё не решено, нужен ли также ключ для Шкатулки Тёмного Кварца). Все имена файлов уже присланы и лежат в `icons/items_pending/`, ждут своего этапа реализации.

**v5.7.19 — донастройка иконок предметов/титулов.**
- **Титул в шапке главного экрана** — вместо emoji теперь маленькая (16px) некликабельная картинка перед названием (`titleDisplay` рендерится через `innerHTML` с `<img>`, ищет id через `TITLE_ICON_IDS[data.activeTitle]`, если не найден — просто текст без иконки).
- **Титул в Инвентаре** — размер увеличен до стандартных 32px (единая CSS-иконка `.item-icon-img`, отдельный класс `.title-icon-img` убран как более ненужный), стал кликабельным (открывает то же превью 200×200, что и обычные предметы). Клик по самой иконке использует `event.stopPropagation()`, чтобы не triggерить заодно клик по всей карточке (выбор титула активным) — это два независимых действия на одной карточке.
- **Магазин** — баланс кредитов вынесен из заголовка в отдельную строку под ним (`#shopBalance`, обновляется в `openShop()` отдельно от `#shopHeader`).
- **Кодекс** — решено НЕ добавлять картинки предметов в записи глав (сознательный выбор: Кодекс остаётся чисто информационным текстом, посмотреть на сам предмет визуально можно в Инвентаре).

**v5.8.0 — Этап 2 предметов: новые артефакты, шкатулки, ключи, расходники.**

**Новые артефакты** (добавлены в `ARTEFACTS`, выдаются строго по уровню через `checkLevelUp()`, тот же принцип, что и у первых трёх — никогда не выпадают из шкатулок):
- `seal_limit` (Печать Предела, ур. 40) — +1 использование Преодоления лимита в день.
- `sphere_growth` (Сфера Роста, ур. 50) — +2 очка статов при левел-апе, **складывается** с `seal_growth` (Печать Развития). Формула в `checkLevelUp()`: база 3, +2 если есть `seal_growth`, +2 если есть `sphere_growth` — максимум 7 при обоих.

**Преодоление лимита — базовый лимит больше не хардкод.** Новая функция `getDailyLimitBreakCap()` = `2 + (есть seal_limit ? 1 : 0) + data.extraLimitBreaksToday`. Используется и в `checkLimitBreak()` (отображение статуса), и в обработчике клика по кнопке — единая точка правды, не дублировать логику лимита где-то ещё.

**Новые расходники** (в `SHOP_CATALOG`, доступны в Магазине + могут выпадать из шкатулок) — выстроены в "лестницы силы", каждый следующий тир **не дублирует**, а превосходит предыдущий:
- Восстановление опыта после штрафа: `potion_restoration` 50% (было) → `rune_restoration_charged` 65% → `crystal_restoration` 80%. Общий обработчик `useRecoveryItem(rate, label)`.
- Буст опыта на день: `potion_growth` +20% (было) → `rune_growth_charged` +35% → `crystal_clarity` +55%. Общий обработчик `useExpBoostItem(rate, label)`.
- Преодоление лимита разово: `shard_limit` +1 использование сегодня, `rune_limit_charged` +2 (оба пишут в `data.extraLimitBreaksToday`, не сбрасывается сразу — суммируется, если использовать несколько).
- `rune_protection_charged` — комбинированный эффект: активирует защиту на завтра (как обычная Руна Защиты) **и**, если сегодня уже есть непогашенный `data.lastExpLoss`, полностью (100%) его возвращает и гасит. Срабатывает частично, если применим только один из двух эффектов.

**`data.expBoostToday` — важное изменение типа.** Раньше был boolean (фиксированный +20%). Теперь **число** (сама ставка буста, 0 = нет буста). Это позволяет разным по силе предметам конкурировать честно: `useExpBoostItem()` не даёт понизить уже активный более сильный буст (`if (data.expBoostToday >= rate) → предупреждение`), но позволяет усилить более слабый до более сильного. Старые сохранения с `expBoostToday: true/false` нормализуются при загрузке (`true` → `0.20`, `false`/отсутствие → `0`) — на случай, если у пользователя реально накопились сохранения с прошлых версий.

**Шкатулки — новые 4 тира**, добавлены level-гейтинг и требование ключа:
| Шкатулка | id | Уровень | Ключ |
|---|---|---|---|
| Обсидиановая | `box_obsidian` | 10 | нет |
| Тёмного Кварца | `box_dark_quartz` | 20 | нет |
| Алая | `box_scarlet` | 30 | `key_scarlet` |
| Багровая | `box_crimson` | 40 | `key_crimson` |

`getAvailableBoxTiers()` теперь фильтрует `BOX_CATALOG` по `data.level >= (box.level || 1)` — определяет, какие тиры вообще могут выпасть за левел-ап на этом уровне игрока (базальтовая/ониксовая — `level:1`, всегда доступны). `rollBoxTier()` — взвешенный случайный выбор среди доступных тиров, без изменений в самом механизме.

**Ключи — новая система** (`KEY_CATALOG`, `data.keys` — объект-счётчик, как у `consumables`/`boxes`, но фактически капается на 1 экземпляр):
- Выпадают **только** из шкатулок, которые сами не требуют ключа (Базальтовая/Ониксовая/Обсидиановая/Тёмного Кварца) — иначе получился бы неразрешимый замкнутый круг. Задаётся через `KEY_DROP_POOLS` (список ключей, которые потенциально может выдать эта шкатулка) + `box.keyDropChance` (вероятность на каждый слот лута).
- **Защита от дублей**: при розыгрыше лута в `performOpenBox()` ключи, которые уже есть в `data.keys` (count > 0), исключаются из пула кандидатов на этот розыгрыш — то есть если ключ уже есть, он просто не может выпасть повторно, пока не будет потрачен.
- **Открытие шкатулки с требованием ключа** (`openBox()`): если `box.requiresKey` задан и `data.keys[тот ключ]` не > 0 — показывает уведомление «Нужен предмет: [название ключа]» и не даёт открыть. При успешном открытии ключ списывается (`performOpenBox()`).
- Ключи отображаются в Инвентаре в собственном разделе «Ключи» (между Шкатулками и Расходниками) — без кнопки действия (сами по себе не используются, только тратятся автоматически при открытии соответствующей шкатулки).

**Лут-таблицы новых шкатулок** — важная оговорка: документ-спецификация описывал Алую/Багровую как дающие в том числе "уникальные расходники", но система уникальных расходников (10 позиций) — это Этап 3, ещё не реализована. Поэтому в `BOX_LOOT_POOLS` для `box_scarlet`/`box_crimson` эти слоты **временно замещены** самыми сильными из уже существующих предметов (заряженные руны, кристаллы) — сохраняя дух "у высоких тиров лут лучше", но не давая буквально то, что описано в документе. Когда дойдём до Этапа 3 (настоящие уникальные расходники) — эти лут-таблицы нужно будет пересмотреть и заменить эти "заглушки" на реальные уникальные предметы.

**Кодекс** — новые главы только для 2 новых артефактов (`artifact_seal_limit`, `artifact_sphere_growth`), как и раньше. Новые расходники и ключи — без отдельных глав, покрываются существующей общей главой «Лавка Системы» (осознанное решение, подтверждено пользователем).

**Защита от дублей артефактов через шкатулки** (описанная в документе-спецификации механика "если все артефакты уже есть — шкатулка выдаёт уникальный расходник вместо артефакта") — **не реализована и не будет**, признана неактуальной: в нашей схеме артефакты никогда не выпадают из шкатулок, только по уровню, так что дублирования там в принципе быть не может.

**16 новых иконок** перемещены из `icons/items_pending/` в `icons/items/` (используются кодом) и добавлены в `APP_SHELL` в `sw.js`: `seal_limit`, `sphere_growth`, `shard_limit`, `rune_restoration_charged`, `rune_growth_charged`, `rune_protection_charged`, `rune_limit_charged`, `crystal_restoration`, `crystal_clarity`, `box_obsidian`, `box_dark_quartz`, `box_scarlet`, `box_crimson`, `key_scarlet`, `key_crimson`.

**Отложено на Этап 3 (v5.9.0):** оставшиеся 3 артефакта (Амулет Непрерывности — ур. 60, Кристалл Прозрения — ур. 70, Кристалл Теней — ур. 80), 10 уникальных расходников (настоящие, не заглушки в лут-таблицах Алой/Багровой), 2 оставшиеся шкатулки (Пурпурная — ур. 55, требует `key_purple`; Шкатулка Теней — ур. 75, требует `key_shadow`) и их ключи. Не забыть при реализации уникальных расходников вернуться к лут-таблицам `box_scarlet`/`box_crimson` и заменить временные заглушки.

**v5.8.1 — три мелких фикса.**
- **Значок активного титула** — заменён с emoji (🛡️) на простой текстовый символ `●` (закрашенный кружок), на том же месте (сразу после названия титула).
- **Баг с тапом по карточке титула.** Тап по иконке (картинке) работал, тап по остальной части карточки (имя/описание) — иногда не срабатывал. Причина: карточка кликабельна целиком, но текстовые элементы внутри неё не были защищены от случайного выделения текста на мобильном (та же проблема, что чинили для кнопок в v5.7.10, только там защиту поставили только на `<button>`, не на кликабельные `<div class="item-card">`). Добавлено `user-select: none` на `.item-card` в целом — фикс автоматически касается любых будущих целиком кликабельных карточек, не только титулов.
- **Названия ключей** — убраны с "Ключ [шкатулки]" на цветовые: `key_scarlet` → «Алый ключ», `key_crimson` → «Багровый ключ» (на будущее для Этапа 3: `key_purple` → «Пурпурный ключ», `key_shadow` → «Ключ тени»).
- **"Усиленные" вместо "заряженные"** — все 4 предмета (`rune_restoration_charged`, `rune_growth_charged`, `rune_protection_charged`, `rune_limit_charged`) переименованы с "(заряженная)" на "(усиленная)" в отображаемом названии. Внутренние id не менялись (только `.name`, как и с прошлыми переименованиями).

**v5.9.0 — Этап 3, финальный: последние артефакты, уникальные расходники, последние шкатулки.** Все 8 артефактов, все 8 шкатулок, все 4 ключа, все 10 уникальных расходников — предметная система полностью укомплектована по исходной спецификации.

**Последние 3 артефакта:**
- `amulet_continuity` (Амулет Непрерывности, ур. 60) — усиливает бонус титула «Целеустремлённый» на 50% (в `getDynamicExp()`: базовый бонус 0.05 умножается на 1.5, если артефакт есть → 0.075).
- `crystal_insight` (Кристалл Прозрения, ур. 70) — постоянно +8% к опыту.
- `crystal_shadow` (Кристалл Теней, ур. 80) — постоянно +15% к кредитам (множитель в блоке расчёта `creds`, вместе с остальными credit-бонусами), +5% к опыту, +3 очка статов при левел-апе (складывается с Печатью Развития и Сферой Роста — максимум все три вместе: 10 очков). Плюс: при **первом** полном выполнении дня (тот же момент, что и обычное уведомление «Выполнено», внутри `if (!data.dailyNotices.complete && wasAllDone)`) — независимые броски 10% на Обсидиановую и 5% на Багровую шкатулку.

**Важное упрощение по сравнению с исходным ТЗ** (согласовано с пользователем): "+15% кредитов при полном выполнении дня" реализовано как **постоянный множитель на все кредиты**, а не как разовый бонус именно в момент завершения дня — иначе потребовалось бы заводить отдельный счётчик "кредитов, заработанных сегодня", которого сейчас нет. Шанс на бонусную шкатулку по-прежнему завязан именно на момент завершения дня.

**Новый каталог `UNIQUE_CATALOG`** — 10 уникальных расходников, обитают в `data.consumables` (та же структура, что обычные), но **не входят в `SHOP_CATALOG`** и потому не продаются в Магазине (цикл в `openShop()` берёт только `SHOP_CATALOG` — специально не трогали, чтобы не пришлось городить фильтрацию). Функция `getConsumableInfo(id)` = `SHOP_CATALOG[id] || UNIQUE_CATALOG[id]` — общая точка поиска предмета по id, используется в Инвентаре, `useItem()`, наименовании лута из шкатулок и валидации бэкапа. `buyItem()`/`openShop()` намеренно продолжают использовать только `SHOP_CATALOG` напрямую.

**10 уникальных расходников и их логика в `performUseItem()`:**
- `rune_redemption` (Искупление) — 50% возврата (через общий `useRecoveryItem`)
- `rune_correction` (Исправление) — 100% возврата + `data.consecutiveDays++` (см. оговорку ниже) + `findMostRecentPenaltyDayKey()` меняет статус этого дня в `data.history` на `'success'`
- `rune_fate_cleansing` (Очищение Судьбы) — убирает до 3 штрафов (`data.totalPenalties`) + возвращает `data.lastExpLoss × 3`
- `rune_cleansing_full` (Полное Очищение) — убирает до 5 штрафов, опыт не трогает
- `potion_restoration_full` (Полное Восстановление) — 100% возврата (через `useRecoveryItem`)
- `shard_limit_double` (Осколок Двойного Предела) — те же +2 к `data.extraLimitBreaksToday`, что и у `rune_limit_charged` (одна ветка `if` на оба id)
- `rune_protection_absolute` (Абсолютная Защита) — комбо: `data.insurance=true` + `data.streakShield=true`, оба независимо, работает частично если один уже активен
- `rune_burden_release` (Снятие Бремени) — снижение целей 45% (см. `targetDiscountRate` ниже)
- `rune_return` (Возврата) — 100% возврата + буст опыта минимум 15% (не даёт понизить уже более сильный)
- `crystal_impulse` (Импульса) — буст опыта 35% + буст кредитов 35% одновременно, через `useExpBoostItem(...,silent=true)` + `useCreditBoostItem(...,silent=true)`, с одним общим уведомлением вместо двух

**Оговорка по Руне Исправления** — "восстановление серии" реализовано как простое `data.consecutiveDays++` (на 1), НЕ как честное восстановление точного значения серии до срыва — это значение нигде не сохраняется в момент срыва, восстановить его буквально невозможно без доп. поля. Если понадобится более точная механика — нужно будет при срыве серии (в `applyPenaltyLogic()`) сохранять `data.consecutiveDays` перед обнулением в отдельное поле (например `data.lastBrokenStreak`), чтобы было что реально восстанавливать.

**Новая инфраструктура под переменную силу эффектов:**
- `data.targetDiscountRate` (число, 0 если нет активной скидки) — заменяет жёстко зашитые 25% в `getDynamicTarget()`. `rune_freedom` ставит 0.25, `rune_burden_release` — 0.45, оба используют паттерн "нельзя понизить уже более сильную скидку" (`(data.targetDiscountRate || 0) < X`).
- `data.creditBoostToday` (число, 0 если нет буста) — новая механика, аналог `data.expBoostToday`, но для дневного множителя кредитов. Используется в блоке расчёта `creds` при выполнении задания.
- `useExpBoostItem()`/`useCreditBoostItem()` получили третий необязательный параметр `silent` — подавляет собственное уведомление функции, когда вызывающий код (например, `crystal_impulse`) хочет показать одно общее уведомление на оба эффекта разом.
- Все новые поля (`targetDiscountRate`, `creditBoostToday`) добавлены в миграцию/дефолты/нормализацию/ежедневный сброс/валидацию бэкапа по тому же шаблону, что уже устоялся для похожих полей.

**Последние 2 шкатулки + 2 ключа:**
| Шкатулка | id | Уровень | Ключ |
|---|---|---|---|
| Пурпурная | `box_purple` | 55 | `key_purple` (Пурпурный ключ) |
| Шкатулка Теней | `box_shadow` | 75 | `key_shadow` (Ключ Теней) |

`KEY_DROP_POOLS` для всех 4 «беcключевых» шкатулок (Базальтовая/Ониксовая/Обсидиановая/Тёмного Кварца) расширены — теперь могут выпасть любые 4 ключа, не только Алый/Багровый.

**Лут-таблицы Алой/Багровой обновлены** — временные заглушки из Этапа 2 (усиленные версии обычных предметов) заменены на настоящие уникальные расходники, как и было запланировано.

**Новая база-глава Кодекса «Шкатулки»** (`boxes`) — доступна сразу, как и остальные 4 базовые главы. Добавлена ретроактивная выдача существующим сохранениям (`if (!data.codexUnlocked.includes('boxes')) data.codexUnlocked.push('boxes');`), т.к. простая замена дефолтного значения через `||` в миграции не сработала бы для сохранений, где `codexUnlocked` уже существовал (был truthy) до этой версии.

**Иконки** — все оставшиеся 17 файлов перенесены из `icons/items_pending/` в `icons/items/`; заодно вычищены 16 случайно задвоенных файлов, оставшихся там с Этапа 1 (сама папка `items_pending` теперь пуста и удалена). **Итог: все 48 запланированных иконок используются кодом.** Все 17 новых добавлены в `APP_SHELL` в `sw.js`.

**Предметная система (шкатулки/артефакты/расходники/ключи) полностью укомплектована согласно исходной спецификации.** Дальнейшее развитие — по запросу пользователя, никаких известных "хвостов" не осталось.

**v5.9.1 — глава Кодекса «Архив», напоминание сохранить прогресс, README.**

- **Новая база-глава Кодекса «Архив»** (`archive`) — доступна сразу, как остальные базовые главы. Добавлена в дефолты/миграцию/нормализацию/валидацию бэкапа, плюс ретроактивная выдача существующим сохранениям (`if (!data.codexUnlocked.includes('archive')) data.codexUnlocked.push('archive');`) — тот же паттерн, что уже использовался для главы «Шкатулки» в v5.9.0.
- **Уведомление-напоминание сохранить прогресс** — показывается после **каждого** левел-апа, без исключений, красным тоном (`'danger'`, та же стилистика, что у баннера штрафа). Текст: «Система рекомендует сохранить прогресс в Архиве. Регулярное сохранение убережёт от случайной потери данных.» Зарегистрировано в `checkLevelUp()` как **последний** `setTimeout(...,4500)` внутри блока `if (leveled)` — благодаря тому, что одинаковые задержки в JS выполняются в порядке регистрации (уже устоявшийся паттерн в этом файле), гарантированно всплывает последним в цепочке всех событий этого левел-апа (после уровня/ранга/лимита/артефакта/шкатулок).
- **README.md** добавлен в репозиторий — описание проекта, возможности, технологии, установка. Сознательно **не упоминает** служебную панель «Длань архитектора» — она предназначена только для тестирования, не для публичной документации.
- **LICENSE** — добавлен (MIT, автор — GitHub-логин `eglantt`), README дополнен дисклеймером о том, что проект — фанатская работа по мотивам Solo Leveling, не аффилирован с правообладателями.

**v5.9.2 — согласование по времени баннера штрафа и стартового уведомления.** Раньше при открытии приложения после пропущенного дня окно штрафа и стартовое уведомление показывались буквально в один момент, накладываясь друг на друга. Новая логика в `checkMissedDays()`:
- Если был штраф — баннер штрафа показывается не сразу, а через **3 секунды** после открытия страницы (`setTimeout(() => showPenaltyBanner(...), 3000)`).
- Стартовое уведомление в этом случае **не** показывается по своей отдельной задержке — вместо этого выставляется флаг `pendingStartupNoticeAfterPenalty = true`, и уведомление показывается через 3 секунды **после того, как пользователь закроет баннер штрафа** (новая функция `closePenaltyOverlay()`, привязана к кнопке «Закрыть» баннера вместо прежнего инлайн-скрытия).
- Если штрафа не было — стартовое уведомление, как и раньше, показывается через 3 секунды от открытия страницы, без изменений в этой ветке.
- Кнопка «Применить штраф сейчас» в «Длани архитектора» **сознательно не тронута** — показывает баннер мгновенно, без задержки, поскольку это инструмент для быстрого тестирования, а не симуляция реального сценария открытия приложения.

**v5.9.3 — постепенное открытие ассортимента Магазина.** Раньше все 13 расходников были доступны в Магазине сразу с 1 уровня. Каждая запись `SHOP_CATALOG` получила поле `level` (порог открытия), `openShop()` фильтрует список (`if (data.level < (item.level||1)) continue;`), `buyItem()` дополнительно проверяет то же условие как защиту. Расписание (шаг 3 → 3 → 3 → 3 → 3 → 5 → 5 → 5 → 5 → 5, последний предмет — ровно на 40 уровне, синхронизировано с уровнями открытия шкатулок/артефактов):

| Уровень | Открывается |
|---|---|
| 1 (старт) | Руна Очищения, Руна Защиты, Зелье Восстановления |
| 3 | Руна Освобождения |
| 6 | Руна Стабильности |
| 9 | Зелье Роста |
| 12 | Осколок Предела |
| 15 | Руна Восстановления (усиленная) |
| 20 | Руна Роста (усиленная) |
| 25 | Руна Защиты (усиленная) |
| 30 | Руна Предела (усиленная) |
| 35 | Кристалл Восстановления |
| 40 | Кристалл Ясности (весь ассортимент открыт) |

**v5.9.5 — аудит текстов уведомлений (21 правка по 5 категориям).** Пользователь попросил найти логические/стилистические нестыковки во всех текстах уведомлений и сверить тон с Кодексом.

**🔴 Реальный баг, не просто стиль:** `useRecoveryItem()`/`useExpBoostItem()`/`useCreditBoostItem()` строили сообщение как `${label} использован(о)` — скобки `(о)` показывались пользователю буквально как текст, ни под какой род не подставлялись (должно быть «использовано»/«использована»/«использован» в зависимости от рода предмета — Зелье/Руна/Кристалл). **Исправление:** сигнатура функций изменена — вместо голого существительного (`label`) теперь передаётся уже полностью готовая, грамматически верная фраза (`verbPhrase`, например `'Руна использована'`), собранная в конкретном месте вызова под конкретный предмет. Все 10 точек вызова обновлены.

**🟡 Канцелярское «закрыто» → «выполнено»/«завершён».** В пуле `complete` и в обработчике `rune_correction`/`rune_fate_cleansing` — везде, где Система говорила о задании как о «закрытом» (регистр техподдержки/тикета, не подходящий под лорный тон Кодекса, где ничего подобного нет). При правке случайно возникла коллизия (два одинаковых текста в пуле `complete`) — исправлено на отдельную, девятую уникальную формулировку «Дневное задание выполнено.» (перекликается с названием главы Кодекса «Дневное Задание»).

**🟡 «Статус задания: …» → без канцелярской рамки.** Убрана техно-калька в духе трекера задач — осталось прямое «Задание выполнено»/«Задание не выполнено».

**🟢 Формулировки сравнения силы эффектов** — «не слабее этого» (двойное отрицание) заменено на прямое «более высокое/уже повышено/уже снижены достаточно».

**🟢 Унификация «Нечего …»** — четыре похожих отказа-предупреждения (снять/очистить/восстановить/исправить нечего) приведены к единому паттерну «[глагол] нечего: [причина]».

Полная таблица «было → стало» по всем 21 пункту — см. историю чата. Отдельно проверено: пул `complete` после правок содержит ровно 9 уникальных текстов (не осталось случайных дублей).

**v5.9.6 — визуальная обратная связь в «Длани архитектора».** Все 10 кнопок-действий панели (`devAddExp`, `devSetLevel`, `devQuickLevel`, `devQuickExp`, `devAddCredits`, `devApplyPenalty`, `devResetPenaltyCount`, `devResetDailyProgress`, `devForceFullReset`, `devResetChronicle`) теперь дают мгновенную визуальную реакцию на тап — новая функция `devFlashThenRun(btn, fn)` добавляет CSS-класс `.dev-btn-flash` (анимация `devBtnFlash` — кратковременный рост яркости + лёгкое увеличение масштаба, 0.4с, работает поверх любого цвета кнопки, в отличие от рамочной подсветки `.item-flash`/`.item-flash-gold`, которая рассчитана на тёмные карточки) и только после этого вызывает саму функцию действия. `onclick="devXxx()"` заменён на `onclick="devFlashThenRun(this, devXxx)"` — передаётся именно ссылка на функцию (без скобок), не её немедленный вызов.

**v5.9.36 — новый предмет «Скрижаль Пересмотра», 50-я и новая иконка (первая новая категория предметов с момента завершения основной предметной системы в v5.9.0).**

**Процесс выбора концепции.** Пользователь хотел предмет для перераспределения уже вложенных очков характеристик. Сначала обсуждали название в уже существующих категориях («Кристалл Перерождения» — отклонено как слишком общее, не называющее эффект; «Руна Перераспределения» — рабочий вариант, но пользователь предпочёл не переиспользовать уже занятые категории, чтобы не пришлось стилистически подстраиваться под уже нарисованные предметы). Предложил 5 совершенно новых категорий (Скрижаль/Матрица/Голографический Модуль/Венец/Куб) — выбрана **Скрижаль**, как единственный некруглый, не самоцветный силуэт в игре.

**Название финализировано как «Скрижаль Пересмотра».** Английское имя файла для генерации иконки уточнено пользователем дважды: сначала `scroll_of_reassessment` (свиток — неверный перевод для каменной таблички), поймано пользователем и исправлено на `tablet_of_reassessment` (правильный термин, как в устоявшемся переводе "Tablets of the Law").

**Параметры, согласованные по ходу обсуждения:**
- Цена: 3700 кредитов (изначально предлагал 3800, вровень с Кристаллами Восстановления/Ясности — пользователь скорректировал на 3700)
- Доступность в Магазине: с 20 уровня
- Тип: расходник (`SHOP_CATALOG`), не артефакт — обоснование: эффект разовый, активный, а не постоянный пассивный, и вся уже существующая инфраструктура расходников подходит без изменений
- Распределение по шкатулкам: по требованию пользователя — доступна и в низкоуровневых шкатулках, но редко (не как единственный источник через Магазин). Добавлена **по одной позиции** в лут-таблицы всех четырёх шкатулок нижнего/среднего тира (Базальтовая/Ониксовая/Обсидиановая/Тёмного Кварца) — не несколько копий в одной таблице, а по одной в каждой; не добавлена в шкатулки высокого тира (Алая и выше), чтобы не задваивать доступность на позднем этапе, где она и так продаётся в Магазине.

**Механика (`performUseItem()`, новая ветка):**
```js
const refunded = Object.values(data.stats).reduce((sum, v) => sum + Math.max(0, v - 10), 0);
if (refunded > 0) {
  data.stats = { str: 10, agi: 10, sta: 10, int: 10, per: 10 };
  data.statPoints += refunded;
  used = true;
  ...
} else {
  showNotice("Пересматривать нечего: очки характеристик ещё не вложены.", 'warn');
}
```
**Добавлена защита от бесполезного использования**, которой не было в изначальном черновике — не по запросу пользователя в этой конкретной сборке, а по уже устоявшемуся в проекте принципу «предмет нельзя тратить впустую» (тот же принцип обсуждался для Клепсидры/интервального протокола ранее). Если все статы уже на стартовых значениях — предмет не расходуется, только предупреждение.

Проверено симуляцией двух сценариев: реальные вложения (12/11/12/13/12 → все статы к 10, 10 очков корректно вернулись в пул) и защитный случай (все статы уже на 10 → `used: false`, ничего не меняется, предмет не потрачен).

**Иконка** — обработана тем же методом «на просвет», что и остальные 49 (пользователь загружал с однородным чёрным фоном). 50-й предмет в игре.

**v5.9.35 — терминальный чекбокс вместо тумблера звука, лорные подсказки к характеристикам.**

**1. Переключатель звука — от прямоугольного тумблера к терминальному чекбоксу.** Пользователь после дополнительного демо (10 цветов чекбокса на выбор — зелёный/голубой/красный/жёлтый/оранжевый/пурпурный/белый/золотой/бирюзовый/розовый, с увеличенной зоной тапа 44×44px под палец) выбрал голубой цвет (`#40c0ff`, цвет интерфейса) вместо изначального зелёного. Класс `.sound-toggle` (трек + плавающий ползунок) полностью заменён на `.sound-checkbox`/`.sound-checkbox-zone` (квадрат с галочкой ✓, появляется/пропадает, кликабельная зона 44×44px вокруг визуально меньшего квадрата 26×26px — сохранена та же забота о зоне тапа, что и в демо). Логика `toggleSound()` не менялась.

**2. Лорные подсказки к пяти характеристикам.** Клик по **названию** стата (тексту «СИЛА»/«ЛОВКОСТЬ» и т.д.) открывает компактное окно с описательным текстом — кнопка «+» рядом не затронута, обёрнута отдельно, ведёт себя как раньше.

**Выбор формата окна — сравнили 4 варианта вживую через демо-файл**, прежде чем остановиться: (A) компактное окно по центру, полупрозрачный фон на весь экран, закрытие тапом мимо; (B) полноэкранное окно, как у Архива/Прогресса, с явной кнопкой «ЗАКРЫТЬ»; (C) аккордеон — раскрытие текста прямо в списке статов, без оверлея; (D) окно, похожее на обычное системное уведомление, но не исчезающее само. Выбран **Вариант A** — обоснование: короткая справочная подсказка не заслуживает веса полноэкранного перехода (B), аккордеон рискует "скакать" по высоте при просмотре нескольких статов подряд и прятать текст ниже видимой области экрана (C), а внешнее сходство с обычным авто-исчезающим уведомлением Системы могло бы создать ложное ожидание, что окно D закроется само.

**Тексты (5, финальные, после нескольких раундов правки пользователем):**
- СИЛА: снижение штрафа
- ЛОВКОСТЬ: шанс бонусных кредитов
- ВЫНОСЛИВОСТЬ: снижение нагрузки + опыт в Пределе (оба эффекта явно названы, по требованию пользователя — "чтобы человек мог планировать, куда вкачивать очки")
- ИНТЕЛЛЕКТ: рост опыта
- ВОСПРИЯТИЕ: удача шкатулок (все три механики — вес тиров, шанс кредитов, смещение лут-таблицы — упомянуты собирательно, без перечисления по отдельности)

Без чисел (по прямому решению пользователя — "это излишне"), без прямого цитирования формул — образное первое предложение + прямое называние эффекта во втором, каждый текст со своим ритмом, не единым шаблоном (в отличие от намеренно единообразных описаний шкатулок/титулов).

**Технически:** новый overlay `#statLoreOverlay` (`z-index:1600`), объект `STAT_LORE` (id стата → заголовок + текст), функции `showStatLore(id)`/`closeStatLore()`. Проверено: все 5 `<span class="stat-label">` получили `onclick`, ни одна из 5 кнопок `.stat-add-btn` не затронута; HTML-баланс — 0 незакрытых тегов; все 5 текстов присутствуют в объекте `STAT_LORE`.

**На будущее (зафиксировано, не в этой сборке):** идея нового предмета (расходник или перезаряжаемый артефакт) с эффектом перераспределения уже вложенных очков характеристик — сброс текущего распределения статов для повторного, осознанного раскидывания.

**v5.9.34 — тексты напоминаний освежены, звук на панели разработчика, визуальный тумблер звука.**

**1. Тексты напоминаний о невыполнении — 6 замен из 17.** Пользователь верно указал, что мой первый черновой вариант правки звучал как маркетинговые слоганы («Промедление обернётся потерей», «Последний шанс закрыть задание сегодня!») — не в тоне голоса Системы. Пересмотрел, сверяясь с уже устоявшимися эталонными фразами («Игрок осознал свою силу», «Система не прощает бездействия» и т.п.) — безличные, короткие констатации фактов, без прямого обращения к игроку и без риторики. Из 9 обычных напоминаний — заменено 5, три оставлены как уже соответствующие тону; из 8 срочных — заменена только 1 (дублировавшая по смыслу первую в списке), остальные 7 уже были в порядке.

**2. Звук на кнопках «Длани архитектора».** Добавлен `playSound('tap')` в начало `sysToolsFlashThenRun()` — единая обёртка, через которую уже проходят все кнопки панели, так что это одна точка подключения, а не правка каждой из ~10 кнопок по отдельности.

**3. Переключатель звука — от текстовой кнопки к визуальному тумблеру.** Собран отдельный демо-файл с 9 вариантами дизайна тумблера (5 + ещё 4 по дополнительной просьбе, включая более угловатые/HUD-стилизованные без круглых форм) — пользователь после обсуждения выбрал **Вариант 2**: прямоугольный трек, плавающий блок-«ползунок», без скруглений, синий/зелёный в цветах остального интерфейса. Первый выбор пользователя (Вариант 1, классический круглый ползунок в стиле мобильных ОС) был пересмотрен самим пользователем как не попадающий в лор — визуальный язык обычных настроек телефона, а не HUD-эстетики игры; согласился с этой правкой и переориентировался на угловатые формы.

Технически — новый CSS-класс `.sound-toggle` (и `.on` состояние), разметка `toggleSound()` не менялась по сути (та же функция, тот же вызов `renderBackupModal()` после переключения — состояние тумблера пересчитывается из `data.soundEnabled` при каждой перерисовке автоматически, отдельно ничего досинхронизировать не пришлось).

**Секция «Звук» перенесена** — была между «Уведомления» и «Экспорт», встала сразу после «Версия», перед «Игрок» (по прямой просьбе пользователя). Проверено: итоговый порядок секций в Архиве — Версия → Звук → Игрок → Уведомления → Экспорт.

**v5.9.33 — звук на предметы/покупку добавлен, звук с Преодоления предела убран.**

**Найден и закрыт пробел** — пользователь заметил, что использование расходников из инвентаря и покупка в Магазине не были в списке 12 звуков из прошлой сборки, хотя это тоже частые, значимые действия.

**Убрано:** звук активации Преодоления предела — пользователь решил, что он скорее портит впечатление (совпадает по времени с перезагрузкой страницы/обнулением заданий), чем помогает, плюс сам звук показался неудачным при живом тестировании. Убран полностью, вместе со всей вспомогательной инфраструктурой, которая была нужна только под него — флаг `data.pendingLimitBreakSound` (миграция/дефолты/нормализация — везде вычищен), отложенный вызов в `finishInit()`, сама запись `limitBreak` в объекте `SOUNDS`. Было 12 звуковых профилей, стало 11.

**Добавлено — переиспользование уже одобренных звуков, а не два новых с нуля:**
- Успешное использование предмета (единая точка `if (used) {...}` внутри `performUseItem()`, срабатывает только после подтверждения в табличке и только если эффект реально применился) → звук `questDone`
- Успешная покупка в Магазине (`buyItem()`) → звук `boxOpen`
- Отказ в обоих случаях (недостаточно кредитов / предмету нечего делать) уже был покрыт существующим звуком `denied` через `showNotice(..., 'warn')` — отдельно ничего добавлять не пришлось

**Рассмотрели и осознанно отклонили:** звук на кнопку «Отмена» в табличках подтверждения (используется в 5 разных местах приложения — предметы/шкатулки/покупка/Предел/ещё одно). Решили не добавлять — «Отмена» семантически означает «ничего не произошло», в отличие от остальных звуков, которые все подтверждают свершившееся действие; частое малозначимое действие рисковало стать таким же раздражающим, каким оказался звук Предела.

**Проверено:** после удаления `limitBreak` — 0 оставшихся ссылок на `pendingLimitBreakSound` и на сам звук нигде в коде; после добавления двух новых точек — все вызовы `playSound()` сверены построчно с реальными ключами объекта `SOUNDS`, ни одного «висячего» вызова несуществующего звука.

**v5.9.32 — звук (Стиль Б, мелодичный), 12 событий, полностью синтезирован кодом, без файлов.**

**Процесс:** сначала собран отдельный демо-файл (не часть основного приложения) с 24 кнопками — 12 событий × 2 стиля (А — резкие ретро-биты на square/sawtooth волнах, Б — мелодичные аккорды на sine-волнах) — пользователь сравнил вживую и выбрал **Стиль Б целиком**, ни одного звука из Стиля А. Демо-файл был чисто рабочим инструментом сравнения, в само приложение не входит.

**Звуковой движок** — `tone(freq, start, duration, type, gainPeak)` и `chord(freqs, ...)` (несколько `tone()` одновременно) поверх нативного `AudioContext`. Разблокировка — по первому клику/тапу в любом месте приложения (`{ once: true }`), тот же принцип, что уже был в присланном пользователем коде Табаты.

**12 звуков, все привязаны к конкретным событиям в коде** (не к визуальным анимациям — на случай, если анимация когда-нибудь изменится, звук не должен от неё зависеть):
| Событие | Функция/место в коде |
|---|---|
| Тап по «+» | Обработчик `.add`, в начале |
| Закрытие упражнения | `data.completed[id] >= target` |
| Полный день (первый раз) | `!data.dailyNotices.complete && wasAllDone` |
| Левел-ап | `checkLevelUp()`, на каждый пройденный уровень |
| Бонус по кредитам | Момент срабатывания `critChance` (BONUS!) |
| Штраф | `computePenaltyAndApply()` |
| Обычная шкатулка | `performOpenBox()` |
| Аномалия — Благословение | `performOpenAnomalyBox()`, ветка благословения |
| Аномалия — Проклятие | `performOpenAnomalyBox()`, ветка проклятия |
| Предел активирован | `limitBreakBtn` — см. отдельный технический нюанс ниже |
| Действие отклонено | `displayNotificationNow()`, тон `warn`/`danger` — единая точка, покрывает **все** отказы разом, без исключений (по решению пользователя) |
| Титул / глава Кодекса | `unlockCodexChapter()` — единая точка для рангов/титулов/артефактов/Предела; **не** продублирована в `unlockTitle()`, хотя та тоже упоминает титул, — `unlockTitle()` сама вызывает `unlockCodexChapter()` внутри, играть дважды подряд не нужно |

**Технический нюанс — звук активации Предела не может звучать напрямую.** Обработчик кнопки завершается `location.reload()` сразу после сохранения — звук, запущенный прямо перед этим, был бы обрублен на середине. Решение — флаг `data.pendingLimitBreakSound`, выставляется перед перезагрузкой, звук доигрывается уже в `finishInit()`, после того как страница загрузится заново. **Честная оговорка, а не гарантия:** свежий `AudioContext` после `reload()` в некоторых браузерах может потребовать нового клика пользователя для разблокировки — звук в этом случае может не прозвучать, без ошибки, просто тихо. Тот же паттерн, что уже применялся для уведомления о находке Шкатулки Аномалии в v5.9.20 (позже упрощённый в v5.9.21, когда тот конкретный триггер переехал в место без `reload()`) — здесь `reload()` неизбежен по самой структуре кнопки, так что паттерн остаётся.

**Небольшое, осознанно принятое наложение звуков:** при выпадении проклятия из Шкатулки Аномалии играет `anomalyCurse` сразу в момент броска, а чуть позже (когда соответствующее уведомление реально отобразится, оно тоже тона `danger`) — вдобавок `denied`. Два звука подряд с разницей в несколько секунд, не одновременно, не считается багом — просто небольшое усиление ощущения "плохой исход", не проверялось на предмет "нужно ли специально гасить".

**Переключатель звука** — новое поле `data.soundEnabled`, **по умолчанию `true`**. Кнопка в Архиве, между разделами «Уведомления» и «Экспорт». При включении звука через переключатель — сразу проигрывается короткий `tap` для мгновенного подтверждения, что звук действительно заработал.

**Проверено (не только заявлено):** HTML-баланс — 0 незакрытых тегов; все 12 ключей `SOUNDS` совпадают один-в-один с 12 использованными `playSound()`-вызовами (ни одного потерянного, ни одного лишнего); симуляция переключателя (`soundEnabled=false` реально блокирует воспроизведение, `true` восстанавливает) — прогнана и подтверждена.

**v5.9.31 — крупный пересчёт баланса ранней прокачки: новая формула опыта до 10 уровня + базовый лимит Преодоления предела снижен до 1 раза в сутки.**

**Как обнаружилась проблема.** Пользователь достиг 3 уровня и заметил, что до следующего требуется всего 450 опыта (`150×3` по старой формуле) — «крайне мало». Проверили: один полностью закрытый день на 3 уровне даёт **572** опыта — то есть **больше**, чем весь переход на 4-й уровень, при 100%-й выработке за один цикл заданий. С активным использованием Преодоления предела (которое раньше можно было использовать до 2 раз в день) 10 уровень набирался всего за **5 дней**.

**Ключевая находка по Пределу — дело не в Выносливости.** Изначально предполагали, что разгон темпа связан с бонусом Выносливости в этом режиме — проверили отдельно и оказалось, что даже **без единого очка**, вложенного в этот стат, один раунд Предела **удваивает** дневной опыт (×2.00 ровно) — просто потому что `getDynamicExp()` не делает разницы между первым и повторным прохождением дня, начисляя одинаковую награду за каждое выполнение. Бонус Выносливости добавляет сверху совсем немного (×2.03 вместо ×2.00).

**Решение по Пределу (сознательное, не техническая правка бага).** Опыт за раунд Предела **не трогали** — по прямому решению пользователя это справедливая награда за реально выполненную работу. Вместо этого — **сам базовый лимит использований снижен с 2 до 1 раза в сутки** (`getDailyLimitBreakCap()`: `cap=2`→`cap=1`, бонусы от артефакта Печать Предела и расходников по-прежнему добавляются сверху). Больше одного раунда в день теперь возможно только через отдельно заработанные/купленные предметы, не бесплатно по умолчанию.

**Формула опыта до 10 уровня переписана с нуля, дважды с первого захода не получилось.** Первая попытка (снижающийся коэффициент от 600× к 150×, точка перехода 10-20 уровень) провалила собственную же проверку — оказалось, что при слишком резком снижении коэффициента итоговое требуемое число опыта могло **падать** на некоторых уровнях внутри диапазона перехода, нарушая условие «растёт всегда». Вторая попытка (добавка поверх растущей линейной базы, а не убывающий коэффициент) уже гарантировала монотонный рост математически — но при попытке точно попасть в `150×10=1500` ровно на границе 10 уровня выяснилось, что для короткого окна (7 шагов, уровни 3-10) физически нет места для сколько-нибудь заметной "щадящей" надбавки: любое значение выше ~1500 на 3 уровне неизбежно потребовало бы **снижения** где-то по пути к 10-му, что запрещено. **Финальное, принятое пользователем решение** — отказаться от сложной "надбавки, убывающей к линейной формуле" в пользу простой арифметической прогрессии:

```js
function getExpToNext(lvl) {
  if (lvl <= 9) return 1000 + 50 * (lvl - 1);
  return Math.floor(150 * lvl);
}
```

Уровни 1-9: 1000, 1050, 1100, ..., 1400 (шаг +50 за уровень, всегда монотонно). С 10 уровня — обычные `150×уровень`, без изменений от прежней формулы. Единственный чуть более крупный шаг — на самой границе (9→10 уровень: +100 вместо +50), но он всё равно растущий, не проблема сам по себе.

**Проверенный результат по темпу** (без использования Предела): 3 уровень — 4 дня, 10 уровень — 18 дней, 100 уровень — **382 дня**, что очень близко к исходной, давно откалиброванной цели (~375 дней). Формула опыта проверена на монотонность программно (не на глаз) — прогнана через ту же самую функцию, что и в реальном коде, не через отдельную копию для теста.

**Глава Кодекса «Преодоление Предела»** — «до двух раз в сутки… сверх плана» → «раз в сутки… может продолжить выполнение» (заодно убрано слово «плана» — было решено ранее избегать этого слова в тексте).

**v5.9.30 — устранено опережающее упоминание термина «Преодоление предела» в двух базовых главах Кодекса.**

Пользователь нашёл нестыковку: главы «Характеристики» и «Шкатулки» доступны с 1 уровня, но обе прямо называли «Преодоление предела» по имени — а сама глава про эту механику открывается только на 3 уровне. Игрок мог встретить термин в двух местах раньше, чем узнать, что это вообще такое.

**Решение** (после нескольких неудачных попыток подобрать образный синоним — все забракованы как громоздкие/оторванные от лора): убрать прямое упоминание термина из обеих ранних глав, переформулировав эффект обычными словами без имени собственного, переиспользуя уже существующий в игре оборот «превышение(-ии) задания» (из самой главы `limit_break`, но не как цитата термина, а как обычное словосочетание). Глава `limit_break` не тронута — по прямому решению пользователя, туда ничего не добавляли.

- «Характеристики»: «…позволяет извлечь больше опыта из Преодоления предела» → «…увеличивает опыт при превышении задания»
- «Шкатулки»: «…Такие шкатулки может принести повышение уровня или Преодоление предела» → «…Такие шкатулки можно найти при повышении уровня или при превышении задания» (заодно исправлена грамматическая нестыковка «шкатулки» + «может» — переход на безличную конструкцию «можно найти»)

Проверено: все оставшиеся в коде упоминания «Преодоление предела» относятся либо к предметам/артефактам, открывающимся **после** 3 уровня (Осколок Предела — 12, Руна Предела усиленная — 30, Печать Предела — 40), либо к самому уведомлению о разблокировке (показывается ровно в момент достижения 3 уровня, не раньше), либо к служебному коду/комментариям, невидимым игроку.

**v5.9.29 — три технические/косметические правки.**

**1. Защита от отрицательного значения для Ловкости.** `critChance = (data.stats.agi - 10) * 0.5` → добавлен `Math.max(0, ...)`, как и у остальных четырёх статов. Сейчас безвредно (в игре нет способа понизить стат ниже стартовых 10), устраняет несогласованность на случай появления такой механики в будущем. Проверено: при AGI=10 результат по-прежнему 0%, поведение на обычных значениях не изменилось.

**2. Досчёт пропущенного суточного сброса при возврате из фона.** Пользователь сообщил о реальном случае: оставил приложение открытым на телефоне, экран погас, обратный отсчёт истёк, но задания не обнулились сами — потребовалась ручная перезагрузка. Причина — браузеры на телефоне замораживают/сильно замедляют `setInterval` при погасшем экране (стандартное поведение ради экономии батареи, не баг браузера), а страница никак не реагировала на момент возвращения видимости. Добавлен `visibilitychange`-обработчик — при возврате видимости страницы принудительно вызывает `checkMissedDays()` + `updateTimer()`, не полагаясь только на секундный таймер. Перед добавлением проверено (не только заявлено): `render()` (вызывается изнутри `checkMissedDays()` при реальном сбросе) трогает только видимость строки долга по штрафам, не закрывает никакие открытые модальные окна; `checkMissedDays()` уже безопасна к повторным вызовам (сам её `while`-цикл — no-op, если сброс не назрел) — это стандартный, уже отработанный в приложении паттерн, не новый риск.

**3. «[CRIT!]» → «BONUS!» в анимации завершения задания.** Тот же класс правки, что уже применялась к «Lvl Up»/«Limit Break» — убран английский игровой жаргон и технические скобки-обёртка. Восклицательный знак сохранён (разовая, быстро исчезающая вспышка анимации — не постоянный текст интерфейса, уместнее энергичнее). Затрагивает только текст, механика (случайный шанс от Ловкости, ×1.5 к кредитам) не менялась. Подтверждено: этот случайный бонус применяется **только** к кредитам, к опыту — никогда (опыт всегда считается детерминированно).

**v5.9.28 — Летопись показывает, какие именно упражнения были выполнены при частичном прогрессе.**

**Новая строка** «Выполнено полностью: {список}.» — появляется в подсказке дня, только если «Прогресс дня» < 100% и список не пустой (то есть не при полном успехе — там и так всё понятно, — и не при полном нуле — нечего перечислять).

**Почему это безопасно для дней со штрафом без всяких оговорок** (в отличие от похожей более ранней правки для честного процента успешных дней): штрафной день по определению означает `!allQuestsDone()` на момент сброса — а раз не все 5 заданий закрыты, кнопка «Преодоление предела» физически недоступна (`canUse = allDone && ...`), значит в такой день режим Предела не мог быть активирован вообще, значит `data.completed` на момент штрафа — это честное, нетронутое состояние дня. Никакой Limit-Break-специфичной логики защиты не потребовалось.

**Технически:** новая функция `computePenaltyAndApply()` (уже существовала, единая точка расчёта штрафа) теперь дополнительно возвращает `completedExercises` — список названий упражнений, реально достигших цели на момент штрафа, читая **уже существующие** названия прямо с карточек заданий (новая общая константа `QUEST_DISPLAY_NAMES`, чтобы не плодить дублирующий список третий раз — уже было по два хардкода `targets`-объекта в `allQuestsDone()`/`countDoneQuests()`, не стали трогать их, просто не добавили четвёртый вариант названий). Сохраняется в `historyEntry.completedExercises`, только для дней со статусом `penalty` (не для `protected` — по прямому запросу пользователя).

**Для сегодняшнего дня** — тот же список считается на лету из `data.completed`, с той же самой защитной оговоркой «только если день ещё не закрыт полностью» (`donePercent < 100`), чтобы не показать нечестный список, если сегодняшний прогресс был обнулён уже начатым, но не завершённым раундом Предела.

Проверено симуляцией трёх сценариев: частичный штрафной день (40%, список из 2 упражнений) — строка появляется корректно; штрафной день с 0% — строка не появляется (нечего перечислять); успешный день на 100% — строка не появляется, даже если бы список технически был непустым (защита через `donePercent < 100`).

**v5.9.27 — переформулировка описаний титулов + смена механики «Преодолевшего предел» с активации на завершение.**

**1. Все 4 «заслуженных» титула переформулированы** — единая схема «что сделал Игрок (с конкретным числом/условием) — что это даёт», без отдельного слова-метки «Бонус:» (эффект теперь второе предложение того же описания, не оторван двоеточием). «Новичок» не тронут (структурно другой — нет ни условия, ни эффекта).

| Титул | Новое описание |
|---|---|
| Пробужденный | «Игрок осознал свою силу, достигнув 10 уровня. Весь получаемый опыт увеличен на 1%.» |
| Целеустремлённый | «Ни один день не был потерян зря — семь дней подряд без единого срыва. Весь получаемый опыт увеличен на 5%.» |
| Преодолевший предел | «Предел преодолён пять раз. Награда в кредитах увеличена на 10%.» |
| Вечный странник | «Сто тысяч шагов остались позади. Опыт за шаги увеличен на 10%.» |

**2. Механика «Преодолевшего предел» — активация заменена на завершение.** Проверили: `data.limitBreaksCount` использовался **только** для условия этого титула, больше нигде в коде — значит, безопасно поменять его смысл целиком, не заводя новое поле. Инкремент перенесён из обработчика **нажатия** кнопки (`limitBreakBtn.onclick`) в точку **реального завершения** раунда (тот же `if (data.limitBreakRoundPending && wasAllDone)`, где уже растёт `data.limitBreaksCompletedToday`). Порог титула (`>= 5`) не менялся числом — только семантика того, что считается. Добавлен поясняющий комментарий прямо у условия разблокировки, чтобы не забыть про смену смысла поля в будущем.

Проверено симуляцией: 3 активации, 2 из которых реально завершены (третья брошена на середине) → `limitBreaksCount = 2`, титул **не** открывается — раньше при той же ситуации он бы уже открылся на первой активации (было `>= 5` активаций, не завершений).

**3. Глава Кодекса «Преодолевший предел»** — «Предел испытан на прочность пять раз» (расплывчато, не отражало требование завершённости) → «Предел преодолён пять раз.» (глагол уже сам по себе подразумевает доведённость до конца).

**v5.9.26 — «Пассивно» → «Постоянный эффект» во всех 8 описаниях артефактов.**

Пользователь усомнился в слове «Пассивно» (канцелярский игровой термин). Разобрали — слово несёт реальную функцию: артефакты в Инвентаре, в отличие от соседних кликабельных карточек (расходники/шкатулки), не имеют кнопки действия — слово прежде объясняло, что эффект работает сам. Заменили на менее канцелярский синоним с тем же смыслом: «Постоянный эффект». Пример: «Постоянный эффект: +10% кредитов за шаги.» Проверено — «Пассивно» не осталось нигде в коде (0 совпадений).

**v5.9.25 — пунктуация и регистр в описаниях артефактов/расходников/уникальных предметов.**

По просьбе пользователя провёл аудит всех 31 описания (8 артефактов + 13 расходников Магазина + 10 уникальных предметов) — по аналогии с недавней чисткой описаний шкатулок. Нашёл два реальных, механических расхождения:

- **Точка в конце** — только 4 из 31 описания заканчивались точкой (те, что редактировались в недавних сборках), остальные 27 — без. Приведено к единому виду, точка везде.
- **Регистр «кредитов»** — единственное расхождение, у артефакта Кристалл Ускорения было «+10% Кредитов» с большой буквы, везде в остальных трёх местах — строчная. Исправлено.

**Три другие категории, поднятые при первом просмотре — пересмотрены и признаны ложной тревогой, ничего не менялось:**
- Похожие формулировки у Искупления/Восстановления/Исправления (50%/100%/100%+доп.эффекты) — это тот же самый принцип "лестницы" с единой структурой фразы, что уже сознательно применялся для обычных расходников (Зелье/Руна/Кристалл Восстановления).
- «Слипшиеся» описания у Возврата/Импульса — при повторном прочтении читаются как естественные связные предложения, не как список.
- Троекратный повтор «Защита от штрафа на завтра» (обычная/усиленная/абсолютная Руна Защиты) — тоже часть намеренной лестницы с наращиваемым эффектом, не случайное дублирование.

Проверено программно (не на глаз): финальный подсчёт — 0 описаний без точки из 31, 0 упоминаний «Кредитов» с большой буквы.

**v5.9.24 — единая формулировка левел-апа, процент вместо «не завершён», чистовые описания всех 9 шкатулок.**

**1. Разночтение «Достигнут»/«Получен» устранено.** Уведомление о левел-апе и строка в Летописи говорили об одном и том же событии по-разному. Унифицировано на «Достигнут N-й уровень.» в обоих местах.

**2. Процент вместо расплывчатого «Последний раунд не завершён».** Пользователь напомнил про свой более ранний, оставшийся без ответа вопрос — почему не показываем результат Преодоления предела в процентах. Оказалось, что нужный процент уже фактически вычисляется: `rawPercent = countDoneQuests() * 20` — это и есть состояние **именно последнего** (возможно, незавершённого) раунда в момент суточного сброса, поскольку каждое нажатие кнопки обнуляет `data.completed`, и дальше он копится заново только в рамках текущего раунда. Раньше это значение вычислялось, использовалось для (честного) `donePercent`, и просто нигде не сохранялось отдельно для случая незавершённого раунда — теперь сохраняется в `historyEntry.limitBreakLastRoundPercent`, только когда `limitBreaksCompleted < limitBreaksActivated`.

**Три случая отображения** (пользователь верно указал, что «последний» неуместно, если активация была всего одна):
- Все раунды завершены (любое количество): «Преодоление предела: N раз.»
- Активирован 1 раз, не завершён: «Преодоление предела: не завершено, выполнено на M%.»
- Больше 1 раза, последний не завершён (единственный физически возможный вид расхождения — кнопка не даёт начать новый раунд, не завершив текущий): «Преодоление предела: N раз, последний раунд выполнен на M%.»

Формат «метка: значение» выбран намеренно — совпадает с уже принятым паттерном остальных строк подсказки («Прогресс дня: N%», «Снято штрафом: N EXP»), без соединительных слов вроде «но»/«и».

**Для сегодняшнего, ещё идущего дня** — то же самое, но на лету: `lbLastRoundPercent = countDoneQuests() * 20`, живое значение, не зависящее от того, закрыт ли день уже сейчас (`donePercent` для дня использует честную логику через `dailyNotices.complete`, а `lbLastRoundPercent` — сырое значение, специально для отражения состояния текущего раунда).

**3. Чистовые описания всех 9 шкатулок** — прежние формулировки оказались черновыми решениями прямо из обсуждения, перенесёнными в код без редактуры: смешение «расходник»/«предмет», необоснованные сравнения («чаще», «изредка», «качеством выше, чем в Базальтовой»), английский игровой сленг («лут»), неясная формулировка требования ключа («требует» — будто шкатулка сама чего-то требует) и оговорка «в игре», звучащая как взгляд со стороны, а не голос Системы изнутри мира. Итоговые тексты — единая терминология («предмет» везде вместо чередования с «расходником»), каждое описание самодостаточно (без сравнений с другими шкатулками), у всех четырёх запертых шкатулок — единообразная конструкция «Заперта. Для открытия нужен [ключ].».

**v5.9.23 — честный «Прогресс дня» + отдельная фиксация Преодоления предела в Летописи.**

**Найденная проблема (пользователь):** если день был честно закрыт полностью, а затем игрок активировал «Преодоление предела» и **не доделал** повторный раунд до конца дня — `data.completed` к моменту суточного сброса оставался в «обнулённом» состоянии (кнопка сама его сбрасывает при активации), а `donePercent` для Летописи снимался именно из этого сырого состояния. В итоге честно выполненный день мог отобразиться как «Прогресс дня: 20%» вместо настоящих 100%.

**Разбор мнимого читерства, поднятого по ходу обсуждения** — оказалось, что буквально описанная лазейка («выполнить одно упражнение из Предела, забить на остальное, получить двойную награду без штрафа») технически невозможна: кнопка недоступна, пока не закрыты все 5 обычных заданий (`allDone` в условии `canUse`), и не открывается повторно, пока не завершён текущий раунд. Реальная проблема оказалась не в «лишней награде», а в честности записи истории.

**Исправление №1 — `donePercent` теперь использует уже существующий флаг `data.dailyNotices.complete`** (он и раньше устанавливался в `true` при **первом** полном закрытии дня и не сбрасывался последующими действиями — просто раньше использовался только для показа уведомления «Выполнено», не для расчёта процента):
```
donePercent = data.dailyNotices.complete ? 100 : (countDoneQuests() * 20)
```
Применено **в двух местах**: и в `checkMissedDays()` (для замороженной записи прошедшего дня), и в `buildDayDetail()` для **сегодняшнего**, ещё идущего дня (тот же самый класс проблемы мог возникнуть и в подсказке текущего дня до наступления сброса — исправлено симметрично, не только для истории).

**Исправление №2 — два новых счётчика, разводящие «активировано» и «реально завершено».** `data.limitBreaksToday` (уже существовал) — растёт при каждом нажатии кнопки. Новый `data.limitBreaksCompletedToday` — растёт **только** в момент, когда раунд реально доведён до конца (то же самое место кода, где уже проверяется завершение раунда для розыгрыша Шкатулки Аномалии — `if (data.limitBreakRoundPending && wasAllDone)`). Оба сбрасываются при суточном сбросе, оба сохраняются в `historyEntry` (`limitBreaksActivated`/`limitBreaksCompleted`), только если активаций было больше 0.

**Разница между «активировано» и «завершено» может быть только 0 или 1** — пользователь верно указал, что мой первый черновой пример («активировано 3, завершено 1») логически невозможен, поскольку кнопка физически блокируется до завершения текущего раунда. Отображение упрощено соответственно:
- Все раунды завершены: «Преодоление предела: N раз.»
- Последний не завершён (единственный возможный вид расхождения): «Преодоление предела: N раз. Последний раунд не завершён.»

**Новая функция `pluralTimes(n)`** — «раз/раза», тот же алгоритм, что и `pluralDays()` (день/дня/дней), просто другие слова. Особенность русского языка: «раз» используется и для 1, и для 5+/11-14 (та же самая словоформа для двух разных грамматических случаев) — отличается только «раза» для 2-4.

Проверено симуляцией трёх сценариев: день с незавершённым вторым раундом Предела (100%, явная пометка о незавершённости), день с обоими раундами завершёнными (100%, без пометки), день без единого использования Предела (строка про Предел вообще не показывается). Всё сошлось с ожиданиями.

**v5.9.22 — обратная связь по проклятию Аномалии, чистка характеристик, README в актуальном состоянии.**

**Найденный пробел (по итогам самостоятельного технического аудита, не запрос пользователя на новую фичу):** `data.curseActiveToday` влиял на реальную математику целей заданий (`getDynamicTarget()`), но нигде не отображался игроку — при активном проклятии цели молча вырастали на 40% без единой визуальной подсказки, почему. Пользователь подтвердил, что это стоит исправить, и предложил более простое решение, чем изначально обсуждавшаяся отдельная строка-индикатор.

**1. Убраны латинские сокращения у характеристик.** «СИЛА (STR)» → «СИЛА» и так для всех пяти — обнаружили при разборе, что сами же ранее чистили весь текст от английского сленга (Lvl Up/Limit Break), а тут оставили латиницу прямо в интерфейсе. Только визуальные подписи, внутренние id (`str`/`agi`/`sta`/`int`/`per`) не менялись.

**2. Красные цифры целей вместо отдельной строки-индикатора.** Новые CSS-классы `.counter.cursed`/`.target.cursed` (тот же красный `#ff5555`, что у долга по штрафам). В `render()` — цикл по всем 5 id заданий, `classList.toggle('cursed', !!data.curseActiveToday)` на `.counter`, и на его `.nextElementSibling` (это и есть `.target` — проверена реальная DOM-структура, они идут строго подряд в разметке). Переключается автоматически, синхронно с самой формулой цели — не отдельная логика, которая могла бы разойтись с реальным состоянием.

**3. Уведомление про аномалию — раз в день, встроено в уже существующий стартовый флаг.** Не заводили отдельный флаг "уже показывали сегодня" — переиспользовали `data.dailyNotices.morning` (тот же, что и у стартового уведомления «Получено задание на день»). Добавлено в **оба** места, где может сработать стартовое уведомление — сразу (`checkMissedDays()`, если не было штрафа) и после закрытия баннера штрафа (`closePenaltyOverlay()`, если был) — иначе на «проклятый» день, следующий сразу за штрафным, уведомление об аномалии не показалось бы вообще. Текст: «Зафиксирована аномалия. Требования дневного задания повышены.» (без слова «проклятие» — тот же нейтральный тон, что и в уведомлении при открытии самой шкатулки).

**Обязательная проверка из плана, выполнена симуляцией (не вручную):** полный трёхдневный цикл — день открытия шкатулки (цель обычная, проклятие ещё не наступило) → день проклятия (цель +40%, класс `.cursed` применён) → день **без** новой шкатулки (цель вернулась к обычной, класс `.cursed` снят) — всё сошлось именно так, как требовалось.

**README.md — устранены 4 накопившихся расхождения с реальным приложением** (найдены при выполнении этой задачи, не связаны с проклятием напрямую):
- Характеристики: «Сила / Ловкость / Интеллект» → все 5, включая Выносливость и Восприятие (устарело с v5.9.9)
- Шкатулки: «8... выпадающих за левел-апы» → «9... большинство выпадает за левел-апы, отдельная непредсказуемая шкатулка может принести как награду, так и временное усложнение задания» (не упоминала Шкатулку Аномалии и её альтернативный источник — Преодоление предела)
- Новый блок «Прогресс» — вся фича v5.9.19 (помесячная статистика) отсутствовала в README
- Раздел «Штрафы и дисциплина» дополнен упоминанием долга по штрафам (механика v5.9.14 тоже нигде не упоминалась)

**v5.9.21 — исправлен момент срабатывания триггера Преодоления предела (в той же сессии, до заливки v5.9.20 на GitHub).**

**Пользователь поймал неточность в понимании самой механики Преодоления предела.** Я дважды описал её неверно в двух разных формулировках подряд, прежде чем разобрался в реальном коде: это не "открытый режим, где можно свободно закрывать задания сколько угодно", а **дискретное, ограниченное действие** — кнопка доступна только после закрытия всех 5 обычных заданий, нажатие обнуляет прогресс и запускает **один конкретный раунд** повторного прохождения тех же 5 заданий (с удвоенной наградой), после которого нужно нажать кнопку заново для следующего раунда (если есть ещё использования).

**Триггер шкатулки перенесён с момента нажатия кнопки на момент завершения раунда.** Было: `Math.random() < 0.05` прямо в обработчике клика по кнопке (до того, как раунд вообще начался). Стало: новый флаг `data.limitBreakRoundPending` — выставляется в `true` при нажатии кнопки (вместо старого сразу-броска), а сам бросок 5% происходит в обработчике клика по заданию, **переиспользуя уже существующую переменную `wasAllDone`** (она и так уже вычислялась там для другой цели — определения момента первого закрытия всех заданий за день, до вызова `checkLevelUp()`): `if (data.limitBreakRoundPending && wasAllDone) { ...roll...; data.limitBreakRoundPending = false; }`.

**Приятный побочный эффект** — раз бросок больше не в обработчике кнопки (который делает `location.reload()` сразу после), а в обработчике клика по заданию (который `reload()` не делает вообще) — весь механизм `data.pendingAnomalyNotice` из v5.9.20 (флаг + проверка в `finishInit()` после перезагрузки) оказался **не нужен и полностью убран**: обычный `setTimeout(...,4500)` теперь просто работает напрямую, без обходных путей.

**Защита от утечки между днями:** если раунд был начат (`limitBreakRoundPending = true`), но не завершён до суточного сброса (игрок не доиграл до конца дня) — флаг явно сбрасывается в `checkMissedDays()`, иначе он мог бы ложно сработать на следующий день при закрытии обычных, никак не связанных с Преодолением предела заданий.

Проверено симуляцией трёх сценариев: (1) обычное закрытие дня без активного раунда — бросок не срабатывает; (2) активация → 4 из 5 заданий раунда закрыто — бросок ещё не срабатывает; 5-е закрыто — срабатывает ровно один раз; (3) повторные закрытия после этого — не срабатывают снова.

**v5.9.20 — Шкатулка Аномалии, 49-я и последняя иконка.**

Новый 9-й тип шкатулки, **полностью вне** обычной системы тиров/весов/лут-таблиц — сознательно, по замыслу с самого начала обсуждения.

**`box_anomaly` в `BOX_CATALOG`** — только `name`/`icon`/`desc`/`level:3`, без `weight`/`minItems`/`maxItems`/лут-полей (они и не нужны, у неё своя отдельная логика открытия). **`getAvailableBoxTiers()` явно исключает `box_anomaly`** — иначе она случайно попала бы в обычный `rollBoxTier()` наравне с прочими тирами.

**Два независимых триггера получения, не связанных с обычной системой шкатулок:**
- **8%** дополнительно при каждом левел-апе (от 3 уровня), **сверх** уже гарантированной обычной шкатулки — добавлено прямо в цикл `checkLevelUp()`, использует уже существующий механизм группировки/уведомлений для `gainedBoxes` (никакого отдельного кода для уведомления не потребовалось — сработало "бесплатно").
- **5%** при каждой активации «Преодоления предела» — **технический нюанс**: обработчик кнопки вызывает `location.reload()` сразу после `saveData()`, так что `setTimeout` там никогда не успел бы сработать. Решение — флаг `data.pendingAnomalyNotice`, проверяется и показывается в `finishInit()` **после** перезагрузки страницы, с задержкой в 1с.

**Открытие — отдельная функция `performOpenAnomalyBox()`, не через общий `performOpenBox()`.** Один бросок 50/50:
- **Проклятие (50%)** — `data.pendingCurse = true`, применяется **не сегодня, а завтра**: при следующем суточном сбросе `checkMissedDays()` переносит `pendingCurse` → `curseActiveToday` (та же строка, что и сброс `dailyTargetLevel`), действует ровно один день, затем `checkMissedDays()` автоматически выставляет `curseActiveToday` в `false` на следующем сбросе (просто присваивая значение из уже сброшенного `pendingCurse`, а не отдельным явным сбросом). `getDynamicTarget()` применяет `×1.40` **поверх** уже применённой скидки от рун/Выносливости (не вместо неё) — `Math.ceil` вместо `Math.floor`, чтобы округление работало в пользу Системы, а не игрока (та же логика наоборот, что у скидок).
- **Благословение (50%)** — три категории: **40% ключ** (Алый/Багровый/Пурпурный/Теней, с защитой от дублей — если уже есть все 4, честно откатывается на кредиты вместо пустого результата), **30% кредиты** (плавающая формула `500 + 30×уровень`, потолок 8000 — специально ниже максимума Шкатулки Теней в 10000, чтобы та оставалась самой щедрой шкатулкой в игре), **30% случайный усиленный расходник** (из уже существующих шести).

**Восприятие сознательно не участвует** ни в шансе получения, ни в исходе открытия — единственная полностью статонезависимая механика в игре, по прямому решению пользователя (чтобы сохранить настоящий, неконтролируемый риск).

**Иконка `box_anomaly.png`** — обработана тем же методом "на просвет", что и остальные 48 (пользователь загружал с однородным чёрным фоном, как и раньше). **Это была последняя из запланированных иконок — теперь все 49 используются кодом.**

Проверено полной симуляцией (не вручную): исключение из обычных тиров на разных уровнях, математика проклятия (с скидкой и без), защита от дублей ключей, значения плавающей формулы кредитов на 8 разных уровнях — всё сошлось с расчётами, которые обсуждались в чате.

**Кодекс** — глава «Шкатулки» дополнена (не создавалась отдельная глава — по аналогии с тем, что и остальные 8 тиров шкатулок не имеют собственных глав, только общая).

**v5.9.19 — помесячный прогресс на экране «ПРОГРЕСС».**

Пользователь захотел добавить прогресс по упражнениям не только за всё время, но и с возможностью смотреть по месяцам. Явно уточнил: Летопись **не трогаем** (там остаётся только % выполнения/статус, никаких чисел по упражнениям), новое хранилище — отдельное и самостоятельное.

**Новое поле `data.dailyExerciseLog`** — объект по датам (ключ — "игровой день", `dateKeyFromTimestamp(data.lastReset)`, тот же принцип, что уже используется для Летописи, учитывает сброс в 5 утра, а не полночь), значение — `{pushups, squats, pullups, press, steps}`. Заполняется **в реальном времени**, тем же самым тапом по «+», что и уже существующий `totalStats[id] += val` (прогресс за всё время) — оба счётчика растут синхронно, один код, два места записи, никакой отдельной логики "закрытия дня" для этого не потребовалось.

**Экран «ПРОГРЕСС»** — блок «за всё время» не тронут. Ниже добавлен новый блок с собственной навигацией по месяцам (переиспользованы существующие CSS-классы `.cal-header`/`.cal-arrow`/`.cal-month-label` от Летописи — визуально идентично, ни одного нового стиля создавать не пришлось), под которой — тот же список из 5 упражнений, но сумма только за выбранный месяц. Текущий месяц считается на лету, включая сегодняшний ещё идущий день (поскольку запись в `dailyExerciseLog` уже происходит в реальном времени).

**«Пустые месяцы не показываем»** — реализовано как **ограничение диапазона навигации**, а не как отдельное пустое состояние экрана. `getEarliestExerciseLogMonth()` находит самый ранний месяц, где реально есть хоть одна запись; `progShiftMonth()` не даёт долистать раньше него (кнопка `‹` получает класс `.disabled`, как и `.cal-arrow.disabled`, который уже был в CSS от Летописи). Если `dailyExerciseLog` совсем пуст (сразу после этого обновления, ни одного дня ещё не записано) — доступен только текущий месяц, обе стрелки соответственно ограничены.

**Состояние навигации запоминается** между открытиями экрана — `progViewYear`/`progViewMonth` инициализируются текущим месяцем только при первом открытии (`=== null`), дальше — сохраняют то место, где пользователь остановился, точно так же, как уже работает у Летописи (`calViewYear`/`calViewMonth`).

**Ограничение, о котором стоит помнить:** данные по упражнениям накапливаются только начиная с этой версии — просмотреть помесячную статистику за месяцы **до** обновления невозможно (данных физически нет), и это ожидаемо, не баг.

**Модель данных** — `data.dailyExerciseLog` добавлен в миграцию/дефолты/нормализацию/валидацию импортируемого бэкапа (валидируется по формату даты и пяти числовым полям, тот же паттерн, что уже применялся для `data.history`).

**v5.9.18 — сообщение об отказе в доступе не пишется в журнал уведомлений + новый текст.**

**Новый необязательный параметр `showNotice(message, tone, skipLog)`** — по умолчанию `false`/не передан (ведёт себя ровно как раньше, ни один из существующих 56 вызовов не изменился и не требовал правки). Прошёл через всю цепочку: `showNotice()` → `enqueueNotification()` → `displayNotificationNow()` → там `logNotification()` теперь вызывается только если `!skipLog`. Проверено симуляцией: обычное уведомление по-прежнему пишется в журнал, уведомление с `skipLog:true` — нет.

**Единственное место, где `skipLog: true` используется** — сообщение о неверном ключе в модалке авторизации «Длани архитектора» (`confirmSysToolsAuth()`). Логика: неудачные попытки подобрать пароль не должны оседать в Архиве как обычные системные события.

**Новый текст сообщения** (двухстрочный): «Ключ не принят Системой.\nДоступ запрещён.» — CSS `.system-popup-body` получил `white-space: pre-line` для поддержки переноса строки внутри всплывающего уведомления (тот же приём, что уже применялся для многострочной подсказки Летописи в v5.9.8). Безопасное добавление — затрагивает только уведомления, где текст реально содержит перевод строки, а такое сейчас только в этом одном сообщении.

**v5.9.17 — ретроактивная выдача главы «Пробужденный» + ключ-защита входа в «Длань архитектора».**

**Часть A — исправлен баг из v5.9.16.** Ретроактивная выдача для «Шкатулок»/«Архива»/«Характеристик» уже была реализована раньше, но для новой главы «Пробужденный» — забыл сделать то же самое. Причина бага (найдена пользователем на реальном сохранении, 16 уровень, глава не появилась): `unlockCodexChapter()` для титула вызывается **только** внутри "titlesUnlocked ещё не содержит этот титул" — ветки `unlockTitle()`, то есть только при **первом** получении. У игроков, получивших титул ДО того, как эта глава появилась в коде — код никогда больше не перепроверяет условие повторно. Добавлена та же ретроактивная проверка, что и у остальных базовых глав: `if (titlesUnlocked.includes('Пробужденный') && !codexUnlocked.includes('title_probuzhdenny')) codexUnlocked.push(...)`.

**Часть B — ключ-защита входа в служебную панель (по стороннему ТЗ).** Раньше 7 тапов по версии в Архиве сразу открывали «Длань архитектора». Теперь между тапами и панелью встала модалка авторизации:
- **Новая модалка** `#sysToolsAuthOverlay` (`z-index:1600` — выше Архива и обычных confirm-окон, чтобы гарантированно оказаться поверх) — заголовок «АВТОРИЗАЦИЯ», текст «Обнаружен запрос к служебному контуру. Введите ключ Архитектора.», поле `type="password"`, кнопки «Отмена»/«Подтвердить», Enter эквивалентен «Подтвердить». Стиль переиспользует уже существующие классы (`.danger-warning-step`, `.danger-text`, `.danger-btn-cancel`, `.confirm-btn-continue`) — ничего нового в CSS не потребовалось.
- **Проверка ключа** — `sha256Hex(SYS_TOOLS_SALT + trim(введённое))` через нативный `crypto.subtle.digest('SHA-256', ...)` (без внешних библиотек), сравнение с константой-хэшем. **Пароль в открытом виде нигде в коде не хранится** — только соль (`SYS_TOOLS_SALT = 'SL:ARCH:2026'`) и итоговый хэш (`SYS_TOOLS_KEY`). Пароль задал сам пользователь, хэш посчитан один раз через код (проверено дважды — через `node:crypto` и через `node:crypto`'s `webcrypto` (тот же API, что `crypto.subtle` в браузере) — оба дают идентичный результат, так что хэш в коде гарантированно совпадёт с тем, что посчитает реальный браузер).
- **При верном ключе** — модалка закрывается, поле очищается, вызывается уже существующий `openSysTools()` без изменений.
- **При неверном ключе** — модалка **остаётся открытой** (сознательное решение пользователя — не нужно заново тапать 7 раз при опечатке), показывается `showNotice('Доступ запрещен.', 'danger')`, поле очищается и получает фокус для немедленного повтора.
- **Повторные 7 тапов, пока модалка уже открыта** — `openSysToolsAuth()` просто возвращает фокус в поле, не открывает вторую модалку поверх первой.
- Закрытие — только «Отмена» или успешный ввод, **без** клика по фону (сознательно, по аналогии с остальными модалками, где есть поле ввода — `confirmOverlay`/`nameOverlay` тоже не закрываются кликом по фону, чтобы случайный тап мимо поля не сбрасывал набранное).
- Весь новый код (HTML модалки, JS-логика, константы) — внутри уже существующих маркеров `SL:SYS-TOOLS`. `#itemPreviewOverlay` не тронут. Имя `openSysTools()` и остальной API панели не менялись.

**Если пароль когда-нибудь понадобится сменить** — посчитать новый хэш той же формулой (`SHA-256(SYS_TOOLS_SALT + новый_пароль)`, например через `crypto.subtle.digest` в консоли браузера или через `node:crypto`) и заменить константу `SYS_TOOLS_KEY` — больше нигде в коде пароль не хранится и трогать не нужно.

**v5.9.16 — глава Кодекса для титула «Пробужденный».**

Обнаружили пробел: из пяти титулов главы в Кодексе были только у трёх (`Целеустремлённый`, `Вечный странник`, `Преодолевший предел`) — «Пробужденный» (выдаётся автоматически на 10 уровне) не имел записи в `TITLE_CODEX_MAP`, хотя формально тоже получен по игровому событию, а не выдан с самого начала (в отличие от «Новичка»). Добавлена новая глава `title_probuzhdenny` и запись в `TITLE_CODEX_MAP`. Никакой другой логики трогать не пришлось — `unlockTitle()` уже универсально проверяет карту и вызывает `unlockCodexChapter()` для любого титула, у которого есть соответствие; игроки, уже получившие титул на 10 уровне раньше — получат и главу автоматически при следующем обновлении статуса (`updateTitleStatus()` перепроверяет условия при каждом рендере, не только один раз при первом получении).

Текст: «Десятый уровень достигнут. Система признаёт в Игроке пробуждённый потенциал — отныне и впредь весь получаемый опыт немного увеличен.»

**v5.9.15 — по итогам стороннего code review: устранение риска рассинхрона формулы штрафа + текстовые правки.**

**Формула штрафа сведена к единственной копии.** Новая функция `computePenaltyAndApply()` — вся математика (`basePen`/`lvlReduction`/`strReduction`/`finalPen`/`loss`/`shortfall`/`expDebt`) и мутация данных теперь существуют **в одном месте**. `applyPenaltyLogic()` (реальный суточный штраф) стал тонкой обёрткой — проверяет условия (`isGoalMet`/`allQuestsDone`) и, если применимо, просто вызывает общую функцию. `applyPenaltyAction()` (кнопка «Применить штраф сейчас» в «Длани архитектора») — тоже просто вызывает её напрямую, без гейтинга (тестовая кнопка обязана срабатывать всегда). Раньше это были две независимые копии формулы — именно этот рассинхрон мы поймали и почини́ли в предыдущей же сессии (в `applyPenaltyAction()` не было логики долга при первой реализации v5.9.14) — теперь рассинхрон физически невозможен, а не просто "правило, которое легко забыть". Побочный эффект (в лучшую сторону): раньше `applyPenaltyAction()` безусловно обнулял `consecutiveDays`, игнорируя `data.streakShield` — теперь, раз использует общую функцию, корректно уважает щит серии, как и настоящий штраф.

**README.md** — «Преодоление лимита» → «Преодоление предела» (пропустили при большом переименовании в v5.9.11, README тогда не входил в область правки).

**Руна Исправления** — описание смягчено: «восстанавливает серию» → «продлевает серию на один день» (честнее отражает реальный эффект — `consecutiveDays++`, не восстановление к значению до срыва, которое нигде не хранится).

**Восприятие («вторая половина пула») и подход к Руне Исправления** — рассмотрены по критике code review, но **сознательно не переделаны**: это не баги, а уже задокументированные в CLAUDE.md компромиссы. Полная честная система рангов редкости для лут-таблиц — самостоятельный заход в дизайн, не точечная правка, отложен до отдельного обсуждения с пользователем.

**v5.9.14 — долг по штрафам, заморозка дневных целей, формулировки рун очищения.**

**Обнаруженная и закрытая дыра в балансе.** Раньше `data.exp = Math.max(0, data.exp - loss)` — если штраф "хотел" снять больше опыта, чем было накоплено, недостача просто пропадала бесследно, а не терялась частично. Проверили симуляцией: раз опыт после штрафа тоже никогда не уходит в минус, попав в состояние "опыт = 0" (например, сразу после левел-апа) — игрок мог пропускать дни **сколько угодно** без единой реальной потери опыта, пока сам не решит вернуться к тренировкам. Единственные последствия в этом состоянии — счётчик `totalPenalties` и сброс серии, оба второстепенные по сравнению с потерей опыта.

**Решение — «долг перед Системой» (`data.expDebt`).** Если штраф превышает доступный опыт — недостача (`shortfall = max(0, loss - data.exp)`) записывается в `data.expDebt`, а не пропадает. Новая функция `gainExp(amount)` — единая точка входа для **любого** начисления опыта в игре (награда за задание, восстановление после штрафа, пополнение через «Длань архитектора» — всего 8 точек вызова переведены на неё, ни одного `data.exp +=` не осталось нигде, кроме самой этой функции): сначала гасит долг, и только остаток (если есть) реально добавляется к `data.exp` и может вызвать левел-ап. Пока долг не закрыт — видимого прогресса на шкале опыта не будет, даже при регулярном выполнении заданий.

**Отображение долга** — новая строка в шапке `#expDebtLine` (внутри `.rank-info`, сразу под «EXP: X / Y»), физически скрыта (`display:none`) и появляется только когда `data.expDebt > 0`, текст «Долг перед Системой: −N EXP». Сама строка «EXP: X / Y» дополнительно получает класс `.exp-in-debt` (красный `#ff5555` с тем же свечением, что у баннера штрафа) на время действия долга — оба визуальных сигнала управляются в `render()`.

**Глава Кодекса «Наказание за Слабость»** дополнена четвёртым предложением про долг, порядок предложений — по финальной правке пользователя (смягчение силой/уровнем — сразу после первого предложения, долг — перед заключительным «Система не прощает бездействия», а не после).

**«Заморозка» дневных целей по уровню на начало суток.** Новое поле `data.dailyTargetLevel` — устанавливается один раз при суточном сбросе (`checkMissedDays()`), равно уровню игрока на этот момент. **4 места**, где раньше цель считалась по живому `data.level`, переведены на `data.dailyTargetLevel`: отображение/разблокировка кнопки «+» на задании (`render()`), проверка «всё выполнено сегодня» (`allQuestsDone()`, `countDoneQuests()` — используются штрафом, Преодолением предела, уведомлением «Выполнено», процентом дня). Порядок в `checkMissedDays()` важен: `donePercent` для **завершающегося** дня снимается **до** обновления `dailyTargetLevel` на новый день — иначе день оценивался бы по целям следующего дня. Награда за опыт (`getDynamicExp`) намеренно не тронута — продолжает считаться по живому уровню, левел-ап посреди дня по-прежнему сразу увеличивает награду, просто больше не увеличивает сами требования уже начатого дня.

**Синхронизация с «Длань архитектора»** — `addExpAction`, `quickExpAction`, `setLevelAction`, `quickLevelAction` дополнены строкой `data.dailyTargetLevel = data.level;`, иначе резкая смена уровня через панель тестирования показывала бы цели под старый, уже неактуальный уровень.

**Формулировки рун очищения** — избегаем и «не влияет на Летопись» (негативная конструкция), и голого «Снижает счётчик...» (слишком похоже на интерфейсный, не системный текст). Финальный вариант — через «память Системы»:
- Руна Очищения: «Стирает 1 штраф из памяти Системы.»
- Руна Полного Очищения: «Стирает 5 штрафов из памяти Системы.»
- Руна Очищения Судьбы: «Стирает 3 штрафа из памяти Системы и возвращает 3× последнюю известную потерю опыта.»
Руна Исправления не тронута — единственный предмет, который по-настоящему переписывает Летопись, в этом и есть её уникальность.

**Пойманный и исправленный баг (в той же сборке, до заливки на GitHub).** Пользователь протестировал на своём компьютере: применение штрафа снимало опыт до нуля, но долг никогда не появлялся, сколько раз штраф ни применялся. Причина — забыл продублировать логику долга (`shortfall`/`expDebt`) во **второй** копии формулы штрафа, в `applyPenaltyAction()` (кнопка «Применить штраф сейчас» в «Длани архитектора») — обновил только «настоящую» `applyPenaltyLogic()`. Мы сами же ранее явно фиксировали правило «держать обе копии формулы в синхроне при любых правках баланса» — и в этот раз сам его нарушил. Исправлено, проверено симуляцией (повторное применение штрафа при 0 опыта корректно копит долг: 540 → 1080 EXP). **Урок на будущее:** при любой правке `applyPenaltyLogic()` — сразу же, в том же заходе, проверять и обновлять `applyPenaltyAction()`, не откладывать и не полагаться на память.

**v5.9.13 — склонение «день/дня/дней» + текст кнопки Предела.**

**Новая функция `pluralDays(n)`** — стандартное склонение по правилу русского языка (1→день, 2-4→дня, 5-20 и 0→дней, с исключением 11-14, работает и для чисел вида 21/22/111/112 и т.д.). Применена в двух местах, где число стоит **перед** словом (настоящее согласование): «Рекорд серии: N {день/дня/дней}» и «N {день/дня/дней} всего» (общая сводка в Летописи).

**Сознательно не тронуто** — по итогам аудита всего файла на похожие места: «штрафов N» и «Осталось использований сегодня: N» (порядок обратный — слово **перед** числом, это лейбл-значение вроде «Очков: 3», не полноценное предложение, склонять не нужно), «серия N дн.» (сокращение, сокращения по-русски не склоняются).

**Текст кнопки после исчерпания Преодоления предела** — «Предел на сегодня преодолён» → «Сегодня больше недоступно».

**v5.9.12 (продолжение) — LICENSE, дисклеймер в README, чистка CLAUDE.md.** Не затрагивает `index.html`/`sw.js`/иконки, версия приложения не бампалась.
- **`LICENSE`** — создан, MIT, `Copyright (c) 2026 eglantt` (GitHub-логин автора).
- **`README.md`** — раздел «Лицензия» дополнен ссылкой на `LICENSE` и дисклеймером: проект — фанатская работа по мотивам Solo Leveling (Chugong), не аффилирован с правообладателями.
- **`CLAUDE.md`** — приведён в соответствие с актуальным состоянием проекта (по внешнему ТЗ, сверенному перед правкой): краткий обзор систем в начале документа обновлён («Преодоление предела» вместо «лимита», «Руна Защиты» вместо устаревшего «Свиток», добавлено упоминание всех 5 характеристик, актуальный список базовых глав Кодекса вместо устаревших «4 базовые»); в исторических записях — обновлены ссылки на переименованные функции служебной панели, отмечено закрытие предметной системы (v5.9.0) и расширения статов (v5.9.9–v5.9.10) там, где раньше они значились как «на будущее»; секция «В процессе / на будущее» в конце документа — переписана под текущий статус; длинная история про баг с модальными окнами (v5.7.3–v5.7.5) сжата до короткого «Уроки (не повторять)» без потери сути.

**v5.9.12 — маскировка служебной панели «Длань архитектора» (по внешнему ТЗ).** Панель остаётся в проекте (нужна для тестирования, будет вырезана только когда-нибудь в будущем перед полностью публичной витриной), но в коде больше не выглядит очевидно как "dev/cheat panel" — ни `dev`-префиксов, ни слов "разработчика"/"временно" в комментариях.

**Единый маркер для будущего вырезания — `SL:SYS-TOOLS`** (вместо старых комментариев «ПАНЕЛЬ РАЗРАБОТЧИКА (ВРЕМЕННО)»), расставлен в трёх местах: HTML (`<!-- SL:SYS-TOOLS:START/END -->`), JS (`/* SL:SYS-TOOLS:START/END */`), CSS (тот же формат, вокруг анимации вспышки кнопок). Когда придёт время вырезать панель — искать по этому токену.

**Важный нюанс, который пришлось поймать до правок:** `#itemPreviewOverlay` (превью предметов по клику, не связано с панелью) физически лежал **внутри** старых HTML-границ панели — если бы просто обернули существующие границы в новые маркеры, превью случайно попало бы в зону будущего вырезания. Вынесен **перед** `SL:SYS-TOOLS:START`, работает независимо.

**Вход в панель (7 тапов по версии за 2с) переведён с инлайн-`onclick` на делегирование событий.** Раньше `onclick="devTapVersion()"` был прямо в шаблонной строке, которой рендерится Архив (`innerHTML`) — то есть был "палящим" прямо в разметке. Теперь версия просто получила стабильный `id="appVersionTag"` без всякого `onclick`, а сам обработчик — `document.getElementById('backupContent').addEventListener('click', ...)` с проверкой `e.target.id === 'appVersionTag'`, повешен один раз в JS-блоке `SL:SYS-TOOLS`. Работает надёжно несмотря на то, что содержимое `#backupContent` целиком пересоздаётся каждый раз при открытии Архива — слушатель висит на стабильном родителе, а не на пересоздаваемом потомке. Логика счётчика тапов (7 тапов ≤ 2 секунды) не менялась, только имена переменных.

**Таблица переименований (все ссылки — HTML id/onclick, JS, CSS — обновлены синхронно):**
| Было | Стало |
|---|---|
| `devPanelOverlay` | `sysToolsOverlay` |
| `openDevPanel` | `openSysTools` |
| `devTapVersion` / `devTapTimes` | `onVersionPulse` / `versionPulseTimes` |
| `devFlashThenRun` | `sysToolsFlashThenRun` |
| `devAddExp` | `addExpAction` |
| `devSetLevel` | `setLevelAction` |
| `devQuickLevel` | `quickLevelAction` |
| `devQuickExp` | `quickExpAction` |
| `devAddCredits` | `addCreditsAction` |
| `devApplyPenalty` | `applyPenaltyAction` |
| `devResetPenaltyCount` | `resetPenaltyCountAction` |
| `devResetDailyProgress` | `resetDailyProgressAction` |
| `devForceFullReset` | `forceFullResetAction` |
| `devResetChronicle` | `resetChronicleAction` |
| `devExpInput`/`devLevelInput`/`devCreditsInput` | `sysExpInput`/`sysLevelInput`/`sysCreditsInput` |
| CSS `.dev-btn-flash` / `@keyframes devBtnFlash` | `.sys-tools-btn-flash` / `@keyframes sysToolsBtnFlash` |

**Не менялось:** заголовок открытой панели («ДЛАНЬ АРХИТЕКТОРА»), тексты кнопок, вся игровая логика/баланс/предметы/Кодекс, анимация `LEVEL UP`, README (панель по-прежнему нигде не упоминается).

**v5.9.11 — большой текстовый аудит (по внешнему детальному ТЗ, сверенному построчно с кодом перед стартом).** Только пользовательский текст — формулы/id/структура данных/CSS не менялись, кроме одной обязательной миграции (см. ниже).

**«Лимит» → «Предел» во всём видимом тексте (12 мест, не 9 — исходное ТЗ недосчитало 3 места в уведомлениях `performUseItem`):** заголовок кнопки, статус «ПРЕДЕЛ СНЯТ: …», «Предел на сегодня преодолён» (не «исчерпан» — правка пользователя, лучше сочетается с самим названием механики), уведомления о разблокировке, диалог подтверждения, 5 описаний/уведомлений предметов (Осколок Предела, Руна Предела усиленная, Осколок Двойного Предела ×2 места, Печать Предела). Глава Кодекса `limit_break` уже была корректна («Преодоление Предела») — не трогалась. Внутренние id (`limit_break`, `seal_limit`, `shard_limit`, `rune_limit_charged`, `limitBreaksCount`, `getDailyLimitBreakCap` и т.д.) — не переименовывались, как и требовалось. Один код-комментарий с «Limit Break» (не пользовательский текст) — оставлен как есть.

**«Целеустремлённый» — унифицировано написание с «ё» во всех 16 местах**, включая внутреннюю логику сравнения (`TITLE_PRIORITY`, `TITLE_CODEX_MAP`, `TITLE_ICON_IDS`, все `data.activeTitle === "..."`, `titlesUnlocked.includes(...)`). **Обязательная миграция добавлена в двух местах** — при обычной загрузке данных и при импорте бэкапа (`sanitizeImportedData()`): если `data.activeTitle`/элемент `data.titlesUnlocked` равен старому написанию «Целеустремленный» (без «ё»), автоматически переводится в «Целеустремлённый». Без этого шага игроки, уже получившие этот титул, потеряли бы его после смены сравниваемых строк — обе точки миграции проверены, что старое написание встречается в коде только внутри самой миграционной логики (сравнение), нигде больше.

**Убрано обращение «вы/у вас/ваши»** (3 места): «Штрафов для удаления нет.» (было «У вас нет штрафов…», 2 вхождения — Руна/Полное Очищение), «Способности Игрока признаны Системой.» (было «Ваши способности…» — сознательно **не** взят вариант «Новый ранг зафиксирован Системой», чтобы не дублировать смысл с уже идущим отдельной строкой заголовком «Новый ранг: {название}»).

**Убран английский игровой сленг** — 3× «Lvl Up» → «при повышении уровня» (описания Печати Развития/Сферы Роста/Кристалла Теней; сама анимация `'LEVEL UP'` намеренно не тронута — визуальный элемент, не текст интерфейса), «Limit Break» в описании титула «Преодолевший предел» → «Преодоление предела». Заодно мелкая полировка формулировок титулов: «7 дней» → «Семь дней», «100,000» → «100 000» (русское форматирование числа), «Вы осознали» → «Игрок осознал».

**Прочее:** «Менее двух часов до сброса. Статус: не выполнено.» → «…Задание не выполнено.» (убрана канцелярская конструкция «Статус: …», та же категория правки, что уже применялась в v5.9.5). «Недостаточно кредитов!» → «Недостаточно кредитов.» (убран восклицательный знак). Описание Руны Исправления сглажено (без изменения процентов/механики). «Награды в Кредитах» → «Награды в кредитах» — оказалось уже покрыто правкой диалога подтверждения Предела, отдельно править не пришлось.

**v5.9.10 — Выносливость получила постоянный эффект (не только в Преодолении лимита).**

Пользователь заметил дисбаланс: из пяти статов только Выносливость работала **исключительно** в опциональном режиме (Преодоление лимита) — если игрок им не пользуется, вложенные очки ничего не давали, в отличие от остальных четырёх статов, полезных всегда. Решение — добавить второй, постоянный эффект поверх уже существующего бонуса в Лимите.

**`getDynamicTarget()` переписана** — постоянное снижение целей заданий от Выносливости:
- `staReduction = min(0.20, bonusSta * 0.001)` — 0.1% за очко, потолок **20%**
- Складывается с уже существующим временным снижением от рун (`targetDiscountRate`, если активно) — но **сумма обоих не может превышать 60%** (`combinedReduction = min(0.60, staReduction + runeReduction)`)
- Ставка намеренно ниже, чем у остальных эффектов Выносливости/Восприятия (0.1% против 2%/0.3%) — снижение целей задания это не абстрактное число, а реальное количество повторений, раздавать его щедро означало бы обесценивать саму суть тренировочного приложения

Референсные цифры (подтягивания, база 40, уровень 30): без вложений — 51, при потолке Выносливости (нужно 200 очков сверх старта, STA=210) — 40, при потолке Выносливости **и** одновременно активной Руне Снятия Бремени — 20 (упирается в общий потолок 60%, а не в сумму 20%+45%=65%).

**Логика игрока, подтверждённая пользователем:** если хочется, наоборот, увеличить нагрузку сверх обычной — для этого уже существует «Преодоление лимита» (полностью добровольная опция). Обе механики не конфликтуют, а взаимно дополняют друг друга.

**Итоговая формула Выносливости:**
- Всегда: −0.1%/очко к целям заданий, потолок −20% (складывается с рунами, общий потолок −60%)
- В режиме «Преодоление лимита»: +2%/очко к опыту, без потолка (как в v5.9.9, не изменилось)

**Кодекс** — глава «Характеристики»: предложение про Выносливость обновлено — «Выносливость снижает повседневную нагрузку и позволяет извлечь больше опыта из Преодоления предела.» (было только про Преодоление предела).

**v5.9.9 — расширение системы характеристик до 5 (Выносливость, Восприятие).**

Порядок на экране «Характеристики» — **Сила → Ловкость → Выносливость → Интеллект → Восприятие** (не по алфавиту и не по порядку добавления в код, порядок задан явно и жёстко в HTML/`updateStatusUI()`).

| Характеристика | id | Сокращение | Эффект | Ставка/очко | Потолок |
|---|---|---|---|---|---|
| Выносливость | `sta` | STA (от Stamina — сверено с первоисточником, не Endurance) | Бонус к опыту, но **только** пока активен режим «Преодоление лимита» (`data.isGoalMet === true`) | +2% | нет |
| Восприятие | `per` | PER | Тройной эффект на "удачу": вес редких тиров при розыгрыше шкатулки за левел-ап, шанс бонусных кредитов из шкатулки, смещение выбора внутри лут-таблицы шкатулки в сторону более редких/сильных позиций | +0.3% на все три сразу | нет |

**Реализация Выносливости** — в `getDynamicExp()`: `if (data.isGoalMet) mul += bonusSta * 0.02`. Проверено: не действует вне Лимита, только пока флаг `isGoalMet` истинен (тот же флаг, что уже даёт ×2 к кредитам в этом режиме).

**Реализация Восприятия** — три места:
1. `rollBoxTier()` — веса всех тиров **кроме** Базальтовой/Ониксовой (сознательно оставлены как "гарантированный пол") умножаются на `(1 + bonusPer*0.003)`.
2. `performOpenBox()` — `effectiveCreditChance = box.creditChance + bonusPer*0.003`.
3. `performOpenBox()` — новая функция `pickFromPool(arr)`: с вероятностью `bonusPer*0.003` выбирает предмет из "второй половины" массива лут-таблицы вместо равномерного выбора по всему массиву. **Важная оговорка** — это приближение, не честная система рангов силы предмета: массивы `BOX_LOOT_POOLS` были составлены по общему принципу "слабее → сильнее", но не проверялись строго на этот счёт. Если понадобится более честная механика — стоит переписать лут-таблицы на явные объекты с полем "сила/редкость" вместо массивов-с-повторами.

**Очки характеристик за левел-ап пересчитаны под 5 статов** (пропорция сохранена, округлено):
| Источник | Было | Стало |
|---|---|---|
| Базово | 3 | **5** |
| Печать Развития (ур.30) | +2 | **+3** |
| Сфера Роста (ур.50) | +2 | **+4** |
| Кристалл Теней (ур.80) | +3 | **+6** |
| Максимум при всех артефактах | 10 | **18** |

Описания артефактов (`ARTEFACTS[...].desc`) обновлены под новые числа.

**Кнопки «+» на экране Характеристик** — раньше не имели вообще никакой визуальной обратной связи (ни на успешный клик, ни на исчерпанные очки — просто нативный `disabled` без кастомного стиля). Добавлено: зелёная вспышка (`.stat-btn-flash`, keyframe `statBtnFlash`, цвет `#40ffaa` — тема роста/успеха, не спутать с голубой `.item-flash` или золотой `.item-flash-gold`) при успешном клике; кастомный стиль `.stat-add-btn:disabled` (`opacity:0.35`, `filter: grayscale(0.6)`, `cursor: not-allowed`) вместо дефолтного вида браузера.

**Автоматический рост статов при левел-апе (как в первоисточнике) — рассмотрели и отклонили.** Пользователь спрашивал про эффект такой механики на баланс; промоделировали 3 варианта (каждый уровень / раз в 5 / раз в 10) — даже самый мягкий вариант (раз в 10 уровней) немного смещает уже откалиброванный потолок штрафа по Силе (~на 20 уровней раньше). Решили: **обойтись без авто-роста**, статы растут только через ручное распределение очков.

**Модель данных** — `data.stats.sta`/`data.stats.per` добавлены в дефолты/миграцию/нормализацию/валидацию бэкапа (default 10, тот же паттерн, что у остальных трёх статов).

**Кодекс:**
- Глава «Характеристики» дополнена двумя предложениями (Восприятие, Выносливость).
- Глава «Дневное Задание» — добавлено упоминание бега: «...пресс и бег, отслеживаемый по количеству пройденных шагов...» (раньше было только «шаги», без всякого упоминания бега — расхождение с первоисточником, где это буквально «Run 10 kilometers»; само упражнение в интерфейсе по-прежнему называется «Шаги» — переименовывать рискованно, слишком много завязано на этом слове в коде).

**v5.9.8 — расширенная подсказка Летописи, правка Кодекса, новая глава «Характеристики».**

**Летопись — многострочная подсказка по дню.** Новая функция `buildDayDetail(dateKey, entry, isToday)` строит текст из массива строк (`join('\n')`), CSS `.cal-tooltip-body` получил `white-space: pre-line` для их отображения. Формат:
```
{дата}
{статус-строка}
Прогресс дня: N%              — только если donePercent известен
Получено опыта: +N EXP        — только если > 0
Снято штрафом: −N EXP         — только если > 0
Получен N-й уровень.          — только если был левел-ап в этот день
```
Статус-строки: «Задание выполнено.» / «Задание не выполнено, получен штраф.» / «Задание не выполнено, штраф предотвращён (Руна Защиты).» — **исправлен старый хвост**: раньше здесь ошибочно значилось «Свиток», хотя предмет переименован в «Руну Защиты» ещё много сборок назад, просто это конкретное место никто не трогал.

**Для сегодняшнего дня** (`isToday=true`) прогресс/опыт считаются на лету: `countDoneQuests()*20` и `data.expGainedToday`. **Для прошедших дней** — читаются из замороженных полей записи `data.history[key]`: `donePercent`, `expGained`, `expLost` (все опциональны — если отсутствуют, соответствующая строка просто не показывается; это касается **всех дней, записанных до этой версии** — для них не было и не может быть этих данных).

**Новое поле `data.expGainedToday`** — копит опыт за текущий день (инкрементируется в обработчике клика по «+», рядом с `data.exp +=`), обнуляется при суточном сбросе. Добавлено в миграцию/дефолты/нормализацию.

**`checkMissedDays()` теперь пишет в `historyEntry`** три новых поля при каждом суточном сбросе: `donePercent` (снимается **до** `data.completed = {}`, безопасно — в отличие от бага с "0%" из v5.7.14, здесь `checkLevelUp()` не вызывается, так что уровень не может задним числом переехать в процессе), `expGained` (из `expGainedToday`, только если >0), `expLost` (равен `result.loss` из `applyPenaltyLogic()`, только для дней со статусом `penalty`). `sanitizeImportedData()` обновлена — валидирует все три новых поля при импорте бэкапа.

**Кодекс — правка «Наказание за Слабость».** Переставлены местами 2-е и 3-е предложения плюс лёгкая переформулировка — раньше смягчение штрафа шло ПЕРЕД «Система не прощает бездействия», что читалось как самопротиворечие. Теперь: правило → его строгость → заслуженное смягчение.
Было: «...Тяжесть наказания снижается с ростом силы и уровня Игрока. Система не прощает бездействия.»
Стало: «...Система не прощает бездействия. Однако с ростом силы и уровня эта тяжесть снижается.»

**Кодекс — новая база-глава «Характеристики»** (`stats`), доступна сразу, как остальные базовые главы (та же схема: дефолты/миграция/нормализация/бэкап + ретроактивная выдача существующим сохранениям через `if (!data.codexUnlocked.includes('stats')) ...push('stats')`). Текст: «При каждом повышении уровня Система вознаграждает Игрока очками характеристик. Сила снижает тяжесть штрафа за невыполненное задание. Ловкость повышает шанс на увеличенную награду в кредитах. Интеллект увеличивает получаемый опыт.»

**На будущее (не в этой сборке):** пользователь планирует расширить систему характеристик до 5 (добавить Выносливость и Чутьё, ближе к первоисточнику — в веб-новелле все 5 статов растут на +1 автоматически при левел-апе, плюс 5 свободных очков на распределение, плюс ещё +3 за выполнение Daily Quest). Требует переосмысления двух новых статов под контекст фитнес-трекера (в оригинале Выносливость/Чутьё — это HP и острота чувств, для тренировочного приложения нужна другая интерпретация эффекта). Механика не проработана, отдельный будущий заход. **Сделано в v5.9.9–v5.9.10** — см. соответствующие записи ниже.

**v5.9.7 — текстовые правки («нагрузка») + бонусные кредиты из шкатулок.**

**Текстовые правки (10 замен, ранее согласованный, отложенный из v5.9.6 план — теперь реализован):** повторяющееся «упражнения»/«задания» заменено на «нагрузку» в описаниях/уведомлениях Руны Освобождения и Руны Снятия Бремени (оба предупреждения об уже активной скидке используют общий текст «Нагрузка уже снижена достаточно.»), статус кнопки Преодоления лимита («Завершите дневное задание» — заменено в обоих местах: статичный HTML-плейсхолдер и JS-логика `checkLimitBreak()`), глава Кодекса «D-Ранг». Шапка главного экрана — единственное число: «ЕЖЕДНЕВНОЕ ЗАДАНИЕ» (подтверждено сверкой с первоисточником — Daily Quest в англоязычной веб-новелле, два независимых источника). Два оставшихся «закрыто» в пуле «Напоминание» (спокойные/настойчивые) — на «не выполнено».

**Бонусные кредиты из шкатулок** — каждый `BOX_CATALOG`-тир получил `creditChance`/`creditAmount`. В `performOpenBox()` — независимый бросок (не уменьшает количество/шанс обычного лута предметов): от 25% / 500 ◈ (Базальтовая) до 5% / 10000 ◈ (Шкатулка Теней), полная таблица по всем 8 тирам — см. историю чата с пользователем. Кредиты зачисляются в `data.credits` сразу, без промежуточного предмета в инвентаре. В общем уведомлении об открытии добавляются отдельным пунктом через запятую в формате **«Кредиты {N} ◈»** (не просто число — по прямому пожеланию пользователя, чтобы было однозначно понятно, что это).

**v5.9.4 — обоснование постепенного Магазина + уведомления о новых товарах.**
- Текст главы Кодекса «Лавка Системы» обновлён — теперь явно объясняет, почему ассортимент растёт с уровнем ("Система открывает более действенные средства по мере того, как убеждается в готовности Игрока их применять"). Эта же формулировка задним числом обосновывает и уже существующие уровневые пороги артефактов/шкатулок — отдельную причину для каждого не придумывали.
- `checkLevelUp()` теперь копит `gainedShopItems` во время `while`-цикла (тот же паттерн, что `gainedBoxes`/`gainedArtefacts`): на каждом пройденном уровне проверяет, у каких записей `SHOP_CATALOG` `level === data.level`, и добавляет их в список. После цикла — уведомление на каждый: `«Лавка Системы: доступен новый товар — [Название].»`, зарегистрировано в общей цепочке `setTimeout(...,4500)` (после шкатулок, перед напоминанием сохранить прогресс).
- Ранги не «присваиваются Системой» (по лору статус/ранг выдаёт внешняя Ассоциация Охотников, а не личная Система игрока) — вместо этого Система **констатирует факт** пересечённого порога («Первый этап эволюции...», «Зафиксирован устойчивый прогресс...»).
- Артефакты не «вручаются» — используется нейтральная формулировка «Получен артефакт: …».
- Избегать сюжетных отсылок к событиям/механикам, которых в приложении физически нет (например, для Shadow Monarch — никаких «теней восстают», это не бой с монстрами; акцент на реальный факт — эволюция через тренировки, абсолютный предел системы измерения).
- Термин «штраф» (не «взыскание») — уже устоявшийся в интерфейсе (заголовок баннера, Летопись), использовать именно его.
- Термин «задание» (не «норма»/«норматив») — уже устоявшийся, использовать именно его.
- Конкретные числа в тексте (проценты бонусов и т.п.) должны точно совпадать с реальными значениями в коде (например, Кольцо Спешки — 10%, Амулет Воли — 15%) — Кодекс это справочник, который не должен врать о механике.

## UI/UX-конвенции, которые важно соблюдать

- **Никаких нативных `alert()`/`confirm()`/`prompt()`** — всё оформлено как стилизованные модалки/баннеры в духе «системных окон» (см. `.status-overlay`, `.system-popup`, `showConfirm()`, `showNotice()`). Если добавляешь новое действие, требующее подтверждения или показывающее результат — используй существующие хелперы, не откатывайся на браузерные диалоги.
- **HUD-эстетика**: тёмная сине-чёрная палитра, шрифт Orbitron для заголовков/лейблов, тонкие голубые угловые «скобки» (`.hud-frame`) на панелях и модалках, приглушённая сетка-текстура на фоне панелей. Золотой цвет зарезервирован под системные объявления (левел-ап, титулы, ранги), не под рутинные элементы.
- **Аккуратно с анимациями** — `@keyframes` (`levelUp`, `completeText`, `flash`, `pulse-bg/text`, `auraPulseSss/Monarch`, `systemPopupIn`) не трогать без явного запроса; в паре версий явно проверялось, что правки логики их не задевают.
- Красный/danger-стиль (`.danger-*` классы) зарезервирован под реально необратимые или предупреждающие действия (сброс прогресса, ошибки). Не использовать для рутинных сообщений.

## Рабочий процесс с пользователем (важно!)

Пользователь **не программист**. Установленное явно правило по ходу разработки: **не писать код без прямой команды пользователя** — сначала обсуждение, план, уточняющие вопросы (что не мешает, а что можно менять — например, дизайн/анимации трогать нельзя без явного разрешения), и только после явного «пиши код» — правки. Это правило нарушалось один раз и было явно зафиксировано пользователем как недопустимое — при работе через Claude Code стоит придерживаться той же логики: сначала план и confirmation, потом implementation, особенно для нетривиальных изменений (баланс, новые механики, структурные правки). Мелкие explicit-командные правки («сделай имя жирным», «сократи до SLDT») можно делать сразу.

При любых изменениях **всегда объяснять пользователю простыми словами**, что и почему меняется — он не разбирается в коде и оценивает результат только по итоговому поведению приложения.

## В процессе / на будущее

### Публичный релиз (статус)
- README и LICENSE — есть (MIT + дисклеймер Solo Leveling в README).
- Служебная панель «Длань архитектора» оставлена для тестов, в коде замаскирована маркером `SL:SYS-TOOLS` (вход: 7 тапов по версии в Архиве). Перед полностью публичной витриной — вырезать все блоки `SL:SYS-TOOLS`.

### Опционально позже
- Кнопка установки PWA через `beforeinstallprompt`.
- Ротация / расширение набора упражнений — обсуждалось, решения нет.
- Отдельная система «Достижения» — отложена в пользу Кодекса.
